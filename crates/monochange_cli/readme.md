# monochange_cli

CLI implementation crate for `monochange`.

The published `monochange` package owns the binary name and delegates to this crate so command implementation can evolve behind a dedicated CLI boundary.
