[templates]
paths = [".templates"]

[padding]
before = 0
after = 0

[exclude]
patterns = ["docs/book/"]
markdown_codeblocks = true

[data]
schemaIndex = { command = "node scripts/schema-versions.ts", format = "text", watch = ["scripts/schema-versions.ts"] }
