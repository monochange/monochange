---
monochange: fix
---

# Render CLI help through clap

The CLI now renders root and command help directly through clap, including shared workflow guidance and snapshots for visible command paths. This keeps help output aligned with the clap command tree while preserving coverage for repository-defined run commands.
