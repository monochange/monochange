{
	"schema_version": "0.4",
	"kind": "monochange.releaseRecord",
	"created_at": "2026-08-29T08:33:48Z",
	"command": "release",
	"version": "0.9.2",
	"versions": {
		"main": "0.9.2"
	},
	"release_targets": [
		{
			"id": "main",
			"kind": "group",
			"version": "0.9.2",
			"version_format": "primary",
			"tag": true,
			"release": true,
			"tag_name": "v0.9.2",
			"members": [
				"@monochange/cli",
				"@monochange/cli-darwin-arm64",
				"@monochange/cli-darwin-x64",
				"@monochange/cli-linux-arm64-gnu",
				"@monochange/cli-linux-arm64-musl",
				"@monochange/cli-linux-x64-gnu",
				"@monochange/cli-linux-x64-musl",
				"@monochange/cli-win32-arm64-msvc",
				"@monochange/cli-win32-x64-msvc",
				"@monochange/skill",
				"monochange",
				"monochange_analysis",
				"monochange_cargo",
				"monochange_changelog",
				"monochange_config",
				"monochange_core",
				"monochange_dart",
				"monochange_deno",
				"monochange_ecmascript",
				"monochange_forgejo",
				"monochange_gitea",
				"monochange_github",
				"monochange_gitlab",
				"monochange_go",
				"monochange_graph",
				"monochange_hosting",
				"monochange_lint",
				"monochange_linting",
				"monochange_npm",
				"monochange_publish",
				"monochange_python",
				"monochange_semver",
				"monochange_telemetry",
				"monochange_test_helpers"
			]
		}
	],
	"released_packages": [
		"@monochange/cli",
		"@monochange/cli-darwin-arm64",
		"@monochange/cli-darwin-x64",
		"@monochange/cli-linux-arm64-gnu",
		"@monochange/cli-linux-arm64-musl",
		"@monochange/cli-linux-x64-gnu",
		"@monochange/cli-linux-x64-musl",
		"@monochange/cli-win32-arm64-msvc",
		"@monochange/cli-win32-x64-msvc",
		"@monochange/skill",
		"monochange",
		"monochange_analysis",
		"monochange_cargo",
		"monochange_changelog",
		"monochange_config",
		"monochange_core",
		"monochange_dart",
		"monochange_deno",
		"monochange_ecmascript",
		"monochange_forgejo",
		"monochange_gitea",
		"monochange_github",
		"monochange_gitlab",
		"monochange_go",
		"monochange_graph",
		"monochange_hosting",
		"monochange_lint",
		"monochange_linting",
		"monochange_npm",
		"monochange_publish",
		"monochange_python",
		"monochange_semver",
		"monochange_telemetry",
		"monochange_test_helpers"
	],
	"changed_files": [
		"Cargo.lock",
		"Cargo.toml",
		"changelog.md",
		"crates/monochange/Cargo.toml",
		"crates/monochange/changelog.md",
		"crates/monochange_analysis/Cargo.toml",
		"crates/monochange_analysis/changelog.md",
		"crates/monochange_cargo/Cargo.toml",
		"crates/monochange_cargo/changelog.md",
		"crates/monochange_changelog/Cargo.toml",
		"crates/monochange_changelog/changelog.md",
		"crates/monochange_config/Cargo.toml",
		"crates/monochange_config/changelog.md",
		"crates/monochange_core/Cargo.toml",
		"crates/monochange_core/changelog.md",
		"crates/monochange_dart/Cargo.toml",
		"crates/monochange_dart/changelog.md",
		"crates/monochange_deno/Cargo.toml",
		"crates/monochange_deno/changelog.md",
		"crates/monochange_ecmascript/Cargo.toml",
		"crates/monochange_ecmascript/changelog.md",
		"crates/monochange_forgejo/Cargo.toml",
		"crates/monochange_forgejo/changelog.md",
		"crates/monochange_gitea/Cargo.toml",
		"crates/monochange_gitea/changelog.md",
		"crates/monochange_github/Cargo.toml",
		"crates/monochange_github/changelog.md",
		"crates/monochange_gitlab/Cargo.toml",
		"crates/monochange_gitlab/changelog.md",
		"crates/monochange_go/Cargo.toml",
		"crates/monochange_go/changelog.md",
		"crates/monochange_graph/Cargo.toml",
		"crates/monochange_graph/changelog.md",
		"crates/monochange_hosting/Cargo.toml",
		"crates/monochange_hosting/changelog.md",
		"crates/monochange_lint/Cargo.toml",
		"crates/monochange_lint/changelog.md",
		"crates/monochange_linting/Cargo.toml",
		"crates/monochange_linting/changelog.md",
		"crates/monochange_npm/Cargo.toml",
		"crates/monochange_npm/changelog.md",
		"crates/monochange_publish/Cargo.toml",
		"crates/monochange_publish/changelog.md",
		"crates/monochange_python/Cargo.toml",
		"crates/monochange_python/changelog.md",
		"crates/monochange_semver/Cargo.toml",
		"crates/monochange_semver/changelog.md",
		"crates/monochange_telemetry/Cargo.toml",
		"crates/monochange_telemetry/changelog.md",
		"crates/monochange_test_helpers/Cargo.toml",
		"crates/monochange_test_helpers/changelog.md",
		"packages/monochange__cli/changelog.md",
		"packages/monochange__cli/package.json",
		"packages/monochange__cli-darwin-arm64/changelog.md",
		"packages/monochange__cli-darwin-arm64/package.json",
		"packages/monochange__cli-darwin-x64/changelog.md",
		"packages/monochange__cli-darwin-x64/package.json",
		"packages/monochange__cli-linux-arm64-gnu/changelog.md",
		"packages/monochange__cli-linux-arm64-gnu/package.json",
		"packages/monochange__cli-linux-arm64-musl/changelog.md",
		"packages/monochange__cli-linux-arm64-musl/package.json",
		"packages/monochange__cli-linux-x64-gnu/changelog.md",
		"packages/monochange__cli-linux-x64-gnu/package.json",
		"packages/monochange__cli-linux-x64-musl/changelog.md",
		"packages/monochange__cli-linux-x64-musl/package.json",
		"packages/monochange__cli-win32-arm64-msvc/changelog.md",
		"packages/monochange__cli-win32-arm64-msvc/package.json",
		"packages/monochange__cli-win32-x64-msvc/changelog.md",
		"packages/monochange__cli-win32-x64-msvc/package.json",
		"packages/monochange__skill/changelog.md",
		"packages/monochange__skill/package.json"
	],
	"package_publications": [
		{
			"package": "@monochange/cli",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-darwin-arm64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-darwin-x64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-arm64-gnu",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-arm64-musl",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-x64-gnu",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-x64-musl",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-win32-arm64-msvc",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-win32-x64-msvc",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/skill",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_analysis",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_cargo",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_changelog",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_config",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_core",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_dart",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_deno",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_ecmascript",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_forgejo",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_gitea",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_github",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_gitlab",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_go",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_graph",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_hosting",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_lint",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_linting",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_npm",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_publish",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_python",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_semver",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_telemetry",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_test_helpers",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.9.2",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		}
	],
	"updated_changelogs": [
		"changelog.md",
		"crates/monochange/changelog.md",
		"crates/monochange_analysis/changelog.md",
		"crates/monochange_cargo/changelog.md",
		"crates/monochange_changelog/changelog.md",
		"crates/monochange_config/changelog.md",
		"crates/monochange_core/changelog.md",
		"crates/monochange_dart/changelog.md",
		"crates/monochange_deno/changelog.md",
		"crates/monochange_ecmascript/changelog.md",
		"crates/monochange_forgejo/changelog.md",
		"crates/monochange_gitea/changelog.md",
		"crates/monochange_github/changelog.md",
		"crates/monochange_gitlab/changelog.md",
		"crates/monochange_go/changelog.md",
		"crates/monochange_graph/changelog.md",
		"crates/monochange_hosting/changelog.md",
		"crates/monochange_lint/changelog.md",
		"crates/monochange_linting/changelog.md",
		"crates/monochange_npm/changelog.md",
		"crates/monochange_publish/changelog.md",
		"crates/monochange_python/changelog.md",
		"crates/monochange_semver/changelog.md",
		"crates/monochange_telemetry/changelog.md",
		"crates/monochange_test_helpers/changelog.md",
		"packages/monochange__cli/changelog.md",
		"packages/monochange__cli-darwin-arm64/changelog.md",
		"packages/monochange__cli-darwin-x64/changelog.md",
		"packages/monochange__cli-linux-arm64-gnu/changelog.md",
		"packages/monochange__cli-linux-arm64-musl/changelog.md",
		"packages/monochange__cli-linux-x64-gnu/changelog.md",
		"packages/monochange__cli-linux-x64-musl/changelog.md",
		"packages/monochange__cli-win32-arm64-msvc/changelog.md",
		"packages/monochange__cli-win32-x64-msvc/changelog.md",
		"packages/monochange__skill/changelog.md"
	],
	"deleted_changesets": [
		".changeset/1787046100-prefer-inline.md",
		".changeset/1791000000-bump-propagation.md",
		".changeset/1791000100-defaults-bump-propagation.md",
		".changeset/cli-step-input-literals.md",
		".changeset/docs-refresh.md",
		".changeset/logo-branding.md"
	],
	"changesets": [
		{
			"path": ".changeset/1787046100-prefer-inline.md",
			"summary": "condense redundant changeset entries with `changesets/prefer-inline`",
			"details": "Change entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5b058f4a0ac3b00236bc71e5d5fffcc25483ed4a",
						"short_sha": "5b058f4",
						"url": "https://github.com/monochange/monochange/commit/5b058f4a0ac3b00236bc71e5d5fffcc25483ed4a",
						"authored_at": "2026-08-27T21:55:24Z",
						"committed_at": "2026-08-27T21:55:24Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#640",
						"title": "feat(lints): add changesets/prefer-inline autofixable rule",
						"url": "https://github.com/monochange/monochange/pull/640",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5b058f4a0ac3b00236bc71e5d5fffcc25483ed4a",
						"short_sha": "5b058f4",
						"url": "https://github.com/monochange/monochange/commit/5b058f4a0ac3b00236bc71e5d5fffcc25483ed4a",
						"authored_at": "2026-08-27T21:55:24Z",
						"committed_at": "2026-08-27T21:55:24Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#640",
						"title": "feat(lints): add changesets/prefer-inline autofixable rule",
						"url": "https://github.com/monochange/monochange/pull/640",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/1791000000-bump-propagation.md",
			"summary": "declare how packages and groups propagate bumps to dependents",
			"details": "Release planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_graph",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_semver",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "4ade2f6224f6e479a9c3b55ec13049972409cd6d",
						"short_sha": "4ade2f6",
						"url": "https://github.com/monochange/monochange/commit/4ade2f6224f6e479a9c3b55ec13049972409cd6d",
						"authored_at": "2026-08-29T05:03:15Z",
						"committed_at": "2026-08-29T05:03:15Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#643",
						"title": "feat(release): add per-package and per-group bump_propagation",
						"url": "https://github.com/monochange/monochange/pull/643",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "4ade2f6224f6e479a9c3b55ec13049972409cd6d",
						"short_sha": "4ade2f6",
						"url": "https://github.com/monochange/monochange/commit/4ade2f6224f6e479a9c3b55ec13049972409cd6d",
						"authored_at": "2026-08-29T05:03:15Z",
						"committed_at": "2026-08-29T05:03:15Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#643",
						"title": "feat(release): add per-package and per-group bump_propagation",
						"url": "https://github.com/monochange/monochange/pull/643",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/1791000100-defaults-bump-propagation.md",
			"summary": "add `[defaults].bump_propagation` and most-specific-first precedence",
			"details": "Bump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_semver",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_graph",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "c8ffa23ee675e62943a26118f3e8a7381f46ee55",
						"short_sha": "c8ffa23",
						"url": "https://github.com/monochange/monochange/commit/c8ffa23ee675e62943a26118f3e8a7381f46ee55",
						"authored_at": "2026-08-29T08:16:35Z",
						"committed_at": "2026-08-29T08:16:35Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#644",
						"title": "feat(release): add [defaults].bump_propagation and most-specific-first precedence",
						"url": "https://github.com/monochange/monochange/pull/644",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "c8ffa23ee675e62943a26118f3e8a7381f46ee55",
						"short_sha": "c8ffa23",
						"url": "https://github.com/monochange/monochange/commit/c8ffa23ee675e62943a26118f3e8a7381f46ee55",
						"authored_at": "2026-08-29T08:16:35Z",
						"committed_at": "2026-08-29T08:16:35Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#644",
						"title": "feat(release): add [defaults].bump_propagation and most-specific-first precedence",
						"url": "https://github.com/monochange/monochange/pull/644",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#643",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/643",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/cli-step-input-literals.md",
			"summary": "accept native TOML booleans and numbers in CLI step inputs",
			"details": "CLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal",
			"targets": [
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "eb1f1312b21cb7b5f22998e8c466f4957f4d0ada",
						"short_sha": "eb1f131",
						"url": "https://github.com/monochange/monochange/commit/eb1f1312b21cb7b5f22998e8c466f4957f4d0ada",
						"authored_at": "2026-08-29T03:51:02+01:00",
						"committed_at": "2026-08-29T03:51:02+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#642",
						"title": "feat: accept native TOML booleans and numbers in CLI step inputs",
						"url": "https://github.com/monochange/monochange/pull/642",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "eb1f1312b21cb7b5f22998e8c466f4957f4d0ada",
						"short_sha": "eb1f131",
						"url": "https://github.com/monochange/monochange/commit/eb1f1312b21cb7b5f22998e8c466f4957f4d0ada",
						"authored_at": "2026-08-29T03:51:02+01:00",
						"committed_at": "2026-08-29T03:51:02+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#642",
						"title": "feat: accept native TOML booleans and numbers in CLI step inputs",
						"url": "https://github.com/monochange/monochange/pull/642",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/docs-refresh.md",
			"summary": "refresh documentation and remove stale CLI references",
			"details": "Updated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_linting",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "1e42679a273e06b5d3e3f2041bc850f1866d1511",
						"short_sha": "1e42679",
						"url": "https://github.com/monochange/monochange/commit/1e42679a273e06b5d3e3f2041bc850f1866d1511",
						"authored_at": "2026-08-21T09:30:12Z",
						"committed_at": "2026-08-21T09:30:12Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#637",
						"title": "docs: refresh documentation and remove stale CLI references",
						"url": "https://github.com/monochange/monochange/pull/637",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "57acc08053e0b34a71cab594bb40f24436a04492",
						"short_sha": "57acc08",
						"url": "https://github.com/monochange/monochange/commit/57acc08053e0b34a71cab594bb40f24436a04492",
						"authored_at": "2026-08-27T20:48:08+01:00",
						"committed_at": "2026-08-27T20:48:08+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#638",
						"title": "docs: prefer inline changeset type shorthand and teach it in the skill",
						"url": "https://github.com/monochange/monochange/pull/638",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#637",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/637",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/logo-branding.md",
			"summary": "add the monochange logo across readme, docs.rs, and the mdBook",
			"details": "Every published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_cargo",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_changelog",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_dart",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_deno",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_ecmascript",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_forgejo",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_gitea",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_github",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_gitlab",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_go",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_graph",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_hosting",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_lint",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_linting",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_npm",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_publish",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_python",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_semver",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_snapshot",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_telemetry",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				},
				{
					"id": "monochange_test_helpers",
					"kind": "package",
					"bump": "none",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "docs"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "18fd9540f37b1ee826404c05376bc84c5ab8a7d2",
						"short_sha": "18fd954",
						"url": "https://github.com/monochange/monochange/commit/18fd9540f37b1ee826404c05376bc84c5ab8a7d2",
						"authored_at": "2026-08-28T13:12:33+01:00",
						"committed_at": "2026-08-28T13:12:33+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#641",
						"title": "feat: add the monochange logo to the readme, docs.rs, and the mdBook",
						"url": "https://github.com/monochange/monochange/pull/641",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "18fd9540f37b1ee826404c05376bc84c5ab8a7d2",
						"short_sha": "18fd954",
						"url": "https://github.com/monochange/monochange/commit/18fd9540f37b1ee826404c05376bc84c5ab8a7d2",
						"authored_at": "2026-08-28T13:12:33+01:00",
						"committed_at": "2026-08-28T13:12:33+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#641",
						"title": "feat: add the monochange logo to the readme, docs.rs, and the mdBook",
						"url": "https://github.com/monochange/monochange/pull/641",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		}
	],
	"changelogs": [
		{
			"owner_id": "main",
			"owner_kind": "group",
			"path": "changelog.md",
			"format": "monochange",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [
					"Grouped release for `main`."
				],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### condense redundant changeset entries with `changesets/prefer-inline`\n_Packages:_ _monochange_, _monochange_config_\n\nChange entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #640](https://github.com/monochange/monochange/pull/640)",
							"#### declare how packages and groups propagate bumps to dependents\n_Packages:_ _monochange_, _monochange_config_, _monochange_core_, _monochange_graph_, _monochange_semver_\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n_Packages:_ _monochange_, _monochange_config_, _monochange_core_, _monochange_graph_, _monochange_semver_\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)",
							"#### accept native TOML booleans and numbers in CLI step inputs\n_Packages:_ _monochange_config_, _monochange_core_\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)"
						]
					},
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### refresh documentation and remove stale CLI references\n_Packages:_ _@monochange/cli_, _monochange_core_, _monochange_linting_\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)",
							"#### declare how packages and groups propagate bumps to dependents\n_Packages:_ _@monochange/skill_\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n_Packages:_ _@monochange/skill_\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)",
							"#### accept native TOML booleans and numbers in CLI step inputs\n_Packages:_ _@monochange/skill_\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)",
							"#### refresh documentation and remove stale CLI references\n_Packages:_ _@monochange/skill_, _monochange_\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)",
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n_Packages:_ _monochange_\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)",
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n_Packages:_ _monochange_analysis_, _monochange_cargo_, _monochange_changelog_, _monochange_config_, _monochange_core_, _monochange_dart_, _monochange_deno_, _monochange_ecmascript_, _monochange_forgejo_, _monochange_gitea_, _monochange_github_, _monochange_gitlab_, _monochange_go_, _monochange_graph_, _monochange_hosting_, _monochange_lint_, _monochange_linting_, _monochange_npm_, _monochange_publish_, _monochange_python_, _monochange_semver_, _monochange_telemetry_, _monochange_test_helpers_\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\nGrouped release for `main`.\n### 🚀 Feature\n\n#### condense redundant changeset entries with `changesets/prefer-inline`\n_Packages:_ _monochange_, _monochange_config_\n\nChange entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #640](https://github.com/monochange/monochange/pull/640)\n\n#### declare how packages and groups propagate bumps to dependents\n_Packages:_ _monochange_, _monochange_config_, _monochange_core_, _monochange_graph_, _monochange_semver_\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n_Packages:_ _monochange_, _monochange_config_, _monochange_core_, _monochange_graph_, _monochange_semver_\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n#### accept native TOML booleans and numbers in CLI step inputs\n_Packages:_ _monochange_config_, _monochange_core_\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### refresh documentation and remove stale CLI references\n_Packages:_ _@monochange/cli_, _monochange_core_, _monochange_linting_\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n\n#### declare how packages and groups propagate bumps to dependents\n_Packages:_ _@monochange/skill_\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n_Packages:_ _@monochange/skill_\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n#### accept native TOML booleans and numbers in CLI step inputs\n_Packages:_ _@monochange/skill_\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)\n\n#### refresh documentation and remove stale CLI references\n_Packages:_ _@monochange/skill_, _monochange_\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n_Packages:_ _monochange_\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n_Packages:_ _monochange_analysis_, _monochange_cargo_, _monochange_changelog_, _monochange_config_, _monochange_core_, _monochange_dart_, _monochange_deno_, _monochange_ecmascript_, _monochange_forgejo_, _monochange_gitea_, _monochange_github_, _monochange_gitlab_, _monochange_go_, _monochange_graph_, _monochange_hosting_, _monochange_lint_, _monochange_linting_, _monochange_npm_, _monochange_publish_, _monochange_python_, _monochange_semver_, _monochange_telemetry_, _monochange_test_helpers_\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange",
			"owner_kind": "package",
			"path": "crates/monochange/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### condense redundant changeset entries with `changesets/prefer-inline`\n\nChange entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #640](https://github.com/monochange/monochange/pull/640)",
							"#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)"
						]
					},
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)",
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### 🚀 Feature\n\n#### condense redundant changeset entries with `changesets/prefer-inline`\n\nChange entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #640](https://github.com/monochange/monochange/pull/640)\n\n#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_analysis",
			"owner_kind": "package",
			"path": "crates/monochange_analysis/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_cargo",
			"owner_kind": "package",
			"path": "crates/monochange_cargo/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_changelog",
			"owner_kind": "package",
			"path": "crates/monochange_changelog/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_config",
			"owner_kind": "package",
			"path": "crates/monochange_config/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### condense redundant changeset entries with `changesets/prefer-inline`\n\nChange entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #640](https://github.com/monochange/monochange/pull/640)",
							"#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)",
							"#### accept native TOML booleans and numbers in CLI step inputs\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)"
						]
					},
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### 🚀 Feature\n\n#### condense redundant changeset entries with `changesets/prefer-inline`\n\nChange entries written as objects can now be flagged and automatically rewritten when they only repeat what the inline form already implies. `monochange check` reports a `changesets/prefer-inline` error for entries such as `bump` plus `type` where the type already implies the same bump, for objects that only declare `type`, and for bare `bump` entries whose bump keyword is also a change type that implies the same bump. `monochange check --fix` collapses them to the concise inline entry. The rule is on by default for every project and is part of the `changesets/recommended` preset.\n\n**Before:**\n\n```markdown\n---\n\"@monochange/cli\":\n  bump: minor\n  type: feat\n---\n```\n\n**After (`monochange check --fix`):**\n\n```markdown\n---\n\"@monochange/cli\": feat\n---\n```\n\nA bare bump entry is converted too when the bump keyword is also a change type that implies the same bump: `\"@monochange/cli\": { bump: minor }` becomes `\"@monochange/cli\": minor`, keeping the bump and gaining the type.\n\nEntries that would change meaning inline are left untouched: explicit `version` values, `caused_by` references, bumps that disagree with the type default, and bare `bump` keywords that are not configured change types.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #640](https://github.com/monochange/monochange/pull/640)\n\n#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n#### accept native TOML booleans and numbers in CLI step inputs\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_core",
			"owner_kind": "package",
			"path": "crates/monochange_core/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)",
							"#### accept native TOML booleans and numbers in CLI step inputs\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)"
						]
					},
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)",
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### 🚀 Feature\n\n#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n#### accept native TOML booleans and numbers in CLI step inputs\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_dart",
			"owner_kind": "package",
			"path": "crates/monochange_dart/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_deno",
			"owner_kind": "package",
			"path": "crates/monochange_deno/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_ecmascript",
			"owner_kind": "package",
			"path": "crates/monochange_ecmascript/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_forgejo",
			"owner_kind": "package",
			"path": "crates/monochange_forgejo/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_gitea",
			"owner_kind": "package",
			"path": "crates/monochange_gitea/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_github",
			"owner_kind": "package",
			"path": "crates/monochange_github/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_gitlab",
			"owner_kind": "package",
			"path": "crates/monochange_gitlab/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_go",
			"owner_kind": "package",
			"path": "crates/monochange_go/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_graph",
			"owner_kind": "package",
			"path": "crates/monochange_graph/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)"
						]
					},
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### 🚀 Feature\n\n#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_hosting",
			"owner_kind": "package",
			"path": "crates/monochange_hosting/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_lint",
			"owner_kind": "package",
			"path": "crates/monochange_lint/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_linting",
			"owner_kind": "package",
			"path": "crates/monochange_linting/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)",
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_npm",
			"owner_kind": "package",
			"path": "crates/monochange_npm/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_publish",
			"owner_kind": "package",
			"path": "crates/monochange_publish/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_python",
			"owner_kind": "package",
			"path": "crates/monochange_python/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_semver",
			"owner_kind": "package",
			"path": "crates/monochange_semver/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)"
						]
					},
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### 🚀 Feature\n\n#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_telemetry",
			"owner_kind": "package",
			"path": "crates/monochange_telemetry/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "monochange_test_helpers",
			"owner_kind": "package",
			"path": "crates/monochange_test_helpers/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### add the monochange logo across readme, docs.rs, and the mdBook\n\nEvery published crate now renders the monochange mark on docs.rs through `html_logo_url`, and docs.rs pages use the matching favicon through `html_favicon_url`. The mark itself is a chunky lowercase `mc` monogram with a version-bump arrow in the negative space.\n\n- the readme gains a top-level hero logo that follows the reader's theme: a light variant on light GitHub themes and a light-on-dark variant on dark themes, using the `picture` element with `prefers-color-scheme`\n- the mdBook in `docs/` picks up a new `favicon.png`\n- `assets/` holds the exported logo sizes (280, 512, 1024), the dark variant, and a multi-size `favicon.ico`\n- a reserve mark (the navy Converge badge) is kept under `assets/reserve/` for a future rebrand\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #641](https://github.com/monochange/monochange/pull/641)\n</details>"
		},
		{
			"owner_id": "@monochange/cli",
			"owner_kind": "package",
			"path": "packages/monochange__cli/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n</details>"
		},
		{
			"owner_id": "@monochange/cli-darwin-arm64",
			"owner_kind": "package",
			"path": "packages/monochange__cli-darwin-arm64/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-darwin-arm64` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-darwin-arm64` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-darwin-x64",
			"owner_kind": "package",
			"path": "packages/monochange__cli-darwin-x64/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-darwin-x64` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-darwin-x64` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-arm64-gnu",
			"owner_kind": "package",
			"path": "packages/monochange__cli-linux-arm64-gnu/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-linux-arm64-gnu` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-linux-arm64-gnu` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-arm64-musl",
			"owner_kind": "package",
			"path": "packages/monochange__cli-linux-arm64-musl/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-linux-arm64-musl` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-linux-arm64-musl` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-x64-gnu",
			"owner_kind": "package",
			"path": "packages/monochange__cli-linux-x64-gnu/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-linux-x64-gnu` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-linux-x64-gnu` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-x64-musl",
			"owner_kind": "package",
			"path": "packages/monochange__cli-linux-x64-musl/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-linux-x64-musl` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-linux-x64-musl` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-win32-arm64-msvc",
			"owner_kind": "package",
			"path": "packages/monochange__cli-win32-arm64-msvc/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-win32-arm64-msvc` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-win32-arm64-msvc` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-win32-x64-msvc",
			"owner_kind": "package",
			"path": "packages/monochange__cli-win32-x64-msvc/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"- No package-specific changes were recorded; `@monochange/cli-win32-x64-msvc` was updated to 0.9.2 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n### Changed\n\n- No package-specific changes were recorded; `@monochange/cli-win32-x64-msvc` was updated to 0.9.2 as part of group `main`."
		},
		{
			"owner_id": "@monochange/skill",
			"owner_kind": "package",
			"path": "packages/monochange__skill/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)",
				"summary": [],
				"sections": [
					{
						"title": "📖 Documentation",
						"collapsed": true,
						"entries": [
							"#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)",
							"#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)",
							"#### accept native TOML booleans and numbers in CLI step inputs\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)",
							"#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)"
						]
					}
				]
			},
			"rendered": "## [0.9.2](https://github.com/monochange/monochange/releases/tag/v0.9.2) (2026-08-29)\n\n<details>\n<summary><strong>📖 Documentation</strong></summary>\n\n#### declare how packages and groups propagate bumps to dependents\n\nRelease planning now supports per-package and per-group `bump_propagation` in `monochange.toml`. A package or group can declare that its changes are matched by dependents (`inherit`), bounded by a maximum (`bump_propagation_max`), pinned to a fixed floor (`none`/`patch`/`minor`/`major`), or left at the workspace `[defaults].parent_bump`. `monochange check` and release planning honor these declarations, and the JSON schema and the monochange skill documentation describe the new fields.\n\n**Before (only the workspace-wide floor existed; a breaking dependency left dependents at `parent_bump`):**\n\n```toml\n[defaults]\nparent_bump = \"patch\"\n```\n\n```markdown\n---\nsdk-core: breaking\n---\n```\n\nPlan: `sdk-core` → major, but every dependent (app, cli) only → patch, even though a breaking dependency is itself breaking for them.\n\n**After (declare inheritance with a clamp, and a floor on another package):**\n\n```toml\n[package.\"@solana/kit\"]\npath = \"crates/kit\"\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n\n[package.\"@solana/leaf\"]\npath = \"crates/leaf\"\nbump_propagation = \"none\"\n```\n\n```markdown\n---\nkit: breaking\n---\n```\n\nRelease plan: `kit` → major, `app` → minor (inherit matches breaking, clamped to minor), and nothing releases for the leaf's dependent. Groups can declare their own propagation, which overrides declarations of member packages, and changesets can still author an explicit bump for a dependent with `caused_by`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #643](https://github.com/monochange/monochange/pull/643)\n\n#### add `[defaults].bump_propagation` and most-specific-first precedence\n\nBump propagation policies now resolve most-specific-first: a package declaration overrides its group declaration, which overrides the new `[defaults].bump_propagation` (with the optional `[defaults].bump_propagation_max` clamp), which overrides the legacy `[defaults].parent_bump` floor.\n\n**Before (only the workspace floor applied to undeclared targets):**\n\n```toml\n[defaults]\nparent_bump = \"major\"\n```\n\nEvery dependent of any changed package had to release major, even when the source's change was a patch.\n\n**After (workspace-wide inherit fallback without redeclaring per package):**\n\n```toml\n[defaults]\nbump_propagation = \"inherit\"\n\n[group.kit]\npackages = [\"kit-core\"]\n\n[package.kit-core]\nbump_propagation = \"inherit\"\nbump_propagation_max = \"minor\"\n```\n\nPrecedence: the most specific declaration wins — the kit-core package clamp (minor) overrides the group's unclamped inherit, which overrides the defaults. Packages and groups with no declaration pick up the defaults inherit; a target can still pin itself with `bump_propagation = \"none\"`.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #644](https://github.com/monochange/monochange/pull/644) · _Related issues:_ [#643](https://github.com/monochange/monochange/issues/643)\n\n#### accept native TOML booleans and numbers in CLI step inputs\n\nCLI step input overrides in `monochange.toml` no longer need to be strings. Authoring `{ draft = true }` (boolean) and `{ jobs = 4, ratio = 2.5 }` (numbers) in a step `inputs` map now parses, and the JSON Schema for the config accepts all three literal shapes:\n\n- booleans keep their native type through parsing and are stringified to `\"true\"`/`\"false\"` when the step runs (unchanged behavior, now covered by tests)\n- numbers are coerced to their string form at parse time, so `{ jobs = 4 }` is exactly `{ jobs = \"4\" }` once the step runs\n- input declarations already accepted boolean and number `default` literals; that behavior is now covered by tests and documented\n- the generated JSON Schema for step input overrides accepts `string`, `boolean`, and `number` values\n- the configuration guide, the Command step reference, and the bundled skill gain an explicit interactive `Command` step example: declare an `interactive` boolean input, pass it to the step, and run the workflow with `--interactive` so the command inherits stdio and owns the terminal\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #642](https://github.com/monochange/monochange/pull/642)\n\n#### refresh documentation and remove stale CLI references\n\nUpdated the guide, reference, crate readme, and agent-facing documentation to match the current CLI model:\n\n- configured `[cli.*]` workflows are documented as `monochange run <name>` commands\n- the removed `mc` binary alias is no longer referenced as an install option\n- stale `monochange <command>` invocations were replaced with the nested `step` or `run` paths\n- generated subagent instructions no longer list the `monochange` executable twice\n- knope migration guide now shows the built-in regex versioned-file support instead of a manual `sed` fallback\n- duplicated `monochange --help` lines and a duplicated registry entry were removed\n- prose was tightened and em dashes were replaced with colons or sentence breaks\n\nThe monochange skill now prefers the inline changeset type shorthand whenever the intended bump matches the type's default bump, and reserves object syntax for overriding a type's default bump.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #637](https://github.com/monochange/monochange/pull/637) · _Related issues:_ [#637](https://github.com/monochange/monochange/issues/637)\n</details>"
		}
	],
	"provider": {
		"kind": "github",
		"owner": "monochange",
		"repo": "monochange",
		"host": null
	}
}
