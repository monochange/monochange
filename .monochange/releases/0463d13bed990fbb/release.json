{
	"schema_version": "0.6",
	"kind": "monochange.releaseRecord",
	"created_at": "2026-09-09T22:36:08Z",
	"command": "release",
	"version": null,
	"versions": {
		"main": "0.11.0",
		"monochange_schema": "0.6.0"
	},
	"release_targets": [
		{
			"id": "main",
			"kind": "group",
			"version": "0.11.0",
			"version_format": "primary",
			"tag": true,
			"release": true,
			"tag_name": "v0.11.0",
			"members": [
				"@monochange/cli",
				"@monochange/cli-darwin-arm64",
				"@monochange/cli-darwin-x64",
				"@monochange/cli-linux-arm64-gnu",
				"@monochange/cli-linux-arm64-musl",
				"@monochange/cli-linux-x64-gnu",
				"@monochange/cli-linux-x64-musl",
				"@monochange/cli-win32-arm64-msvc",
				"@monochange/cli-win32-x64-msvc",
				"@monochange/skill",
				"monochange",
				"monochange_analysis",
				"monochange_cargo",
				"monochange_changelog",
				"monochange_config",
				"monochange_core",
				"monochange_dart",
				"monochange_deno",
				"monochange_ecmascript",
				"monochange_forgejo",
				"monochange_gitea",
				"monochange_github",
				"monochange_gitlab",
				"monochange_go",
				"monochange_graph",
				"monochange_hosting",
				"monochange_lint",
				"monochange_linting",
				"monochange_npm",
				"monochange_publish",
				"monochange_python",
				"monochange_semver",
				"monochange_telemetry",
				"monochange_test_helpers"
			]
		},
		{
			"id": "monochange_schema",
			"kind": "package",
			"version": "0.6.0",
			"version_format": "namespaced",
			"tag": true,
			"release": true,
			"tag_name": "monochange_schema/v0.6.0",
			"members": [
				"monochange_schema"
			]
		}
	],
	"released_packages": [
		"@monochange/cli",
		"@monochange/cli-darwin-arm64",
		"@monochange/cli-darwin-x64",
		"@monochange/cli-linux-arm64-gnu",
		"@monochange/cli-linux-arm64-musl",
		"@monochange/cli-linux-x64-gnu",
		"@monochange/cli-linux-x64-musl",
		"@monochange/cli-win32-arm64-msvc",
		"@monochange/cli-win32-x64-msvc",
		"@monochange/skill",
		"monochange",
		"monochange_analysis",
		"monochange_cargo",
		"monochange_changelog",
		"monochange_config",
		"monochange_core",
		"monochange_dart",
		"monochange_deno",
		"monochange_ecmascript",
		"monochange_forgejo",
		"monochange_gitea",
		"monochange_github",
		"monochange_gitlab",
		"monochange_go",
		"monochange_graph",
		"monochange_hosting",
		"monochange_lint",
		"monochange_linting",
		"monochange_npm",
		"monochange_publish",
		"monochange_python",
		"monochange_schema",
		"monochange_semver",
		"monochange_telemetry",
		"monochange_test_helpers"
	],
	"changed_files": [
		"Cargo.lock",
		"Cargo.toml",
		"changelog.md",
		"crates/monochange/Cargo.toml",
		"crates/monochange/changelog.md",
		"crates/monochange_analysis/Cargo.toml",
		"crates/monochange_analysis/changelog.md",
		"crates/monochange_cargo/Cargo.toml",
		"crates/monochange_cargo/changelog.md",
		"crates/monochange_changelog/Cargo.toml",
		"crates/monochange_changelog/changelog.md",
		"crates/monochange_config/Cargo.toml",
		"crates/monochange_config/changelog.md",
		"crates/monochange_core/Cargo.toml",
		"crates/monochange_core/changelog.md",
		"crates/monochange_dart/Cargo.toml",
		"crates/monochange_dart/changelog.md",
		"crates/monochange_deno/Cargo.toml",
		"crates/monochange_deno/changelog.md",
		"crates/monochange_ecmascript/Cargo.toml",
		"crates/monochange_ecmascript/changelog.md",
		"crates/monochange_forgejo/Cargo.toml",
		"crates/monochange_forgejo/changelog.md",
		"crates/monochange_gitea/Cargo.toml",
		"crates/monochange_gitea/changelog.md",
		"crates/monochange_github/Cargo.toml",
		"crates/monochange_github/changelog.md",
		"crates/monochange_gitlab/Cargo.toml",
		"crates/monochange_gitlab/changelog.md",
		"crates/monochange_go/Cargo.toml",
		"crates/monochange_go/changelog.md",
		"crates/monochange_graph/Cargo.toml",
		"crates/monochange_graph/changelog.md",
		"crates/monochange_hosting/Cargo.toml",
		"crates/monochange_hosting/changelog.md",
		"crates/monochange_lint/Cargo.toml",
		"crates/monochange_lint/changelog.md",
		"crates/monochange_linting/Cargo.toml",
		"crates/monochange_linting/changelog.md",
		"crates/monochange_npm/Cargo.toml",
		"crates/monochange_npm/changelog.md",
		"crates/monochange_publish/Cargo.toml",
		"crates/monochange_publish/changelog.md",
		"crates/monochange_python/Cargo.toml",
		"crates/monochange_python/changelog.md",
		"crates/monochange_schema/Cargo.toml",
		"crates/monochange_schema/changelog.md",
		"crates/monochange_semver/Cargo.toml",
		"crates/monochange_semver/changelog.md",
		"crates/monochange_telemetry/Cargo.toml",
		"crates/monochange_telemetry/changelog.md",
		"crates/monochange_test_helpers/Cargo.toml",
		"crates/monochange_test_helpers/changelog.md",
		"packages/monochange__cli/changelog.md",
		"packages/monochange__cli/package.json",
		"packages/monochange__cli-darwin-arm64/changelog.md",
		"packages/monochange__cli-darwin-arm64/package.json",
		"packages/monochange__cli-darwin-x64/changelog.md",
		"packages/monochange__cli-darwin-x64/package.json",
		"packages/monochange__cli-linux-arm64-gnu/changelog.md",
		"packages/monochange__cli-linux-arm64-gnu/package.json",
		"packages/monochange__cli-linux-arm64-musl/changelog.md",
		"packages/monochange__cli-linux-arm64-musl/package.json",
		"packages/monochange__cli-linux-x64-gnu/changelog.md",
		"packages/monochange__cli-linux-x64-gnu/package.json",
		"packages/monochange__cli-linux-x64-musl/changelog.md",
		"packages/monochange__cli-linux-x64-musl/package.json",
		"packages/monochange__cli-win32-arm64-msvc/changelog.md",
		"packages/monochange__cli-win32-arm64-msvc/package.json",
		"packages/monochange__cli-win32-x64-msvc/changelog.md",
		"packages/monochange__cli-win32-x64-msvc/package.json",
		"packages/monochange__skill/changelog.md",
		"packages/monochange__skill/package.json"
	],
	"package_publications": [
		{
			"package": "@monochange/cli",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-darwin-arm64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-darwin-x64",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-arm64-gnu",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-arm64-musl",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-x64-gnu",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-linux-x64-musl",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-win32-arm64-msvc",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/cli-win32-x64-msvc",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "@monochange/skill",
			"ecosystem": "npm",
			"registry": "npm",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_analysis",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_cargo",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_changelog",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_config",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_core",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_dart",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_deno",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_ecmascript",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_forgejo",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_gitea",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_github",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_gitlab",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_go",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_graph",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_hosting",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_lint",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_linting",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_npm",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_publish",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_python",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_schema",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.6.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_semver",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_telemetry",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		},
		{
			"package": "monochange_test_helpers",
			"ecosystem": "cargo",
			"registry": "crates_io",
			"version": "0.11.0",
			"mode": "builtin",
			"trusted_publishing": {
				"enabled": true,
				"repository": null,
				"workflow": null,
				"environment": null
			}
		}
	],
	"updated_changelogs": [
		"changelog.md",
		"crates/monochange/changelog.md",
		"crates/monochange_analysis/changelog.md",
		"crates/monochange_cargo/changelog.md",
		"crates/monochange_changelog/changelog.md",
		"crates/monochange_config/changelog.md",
		"crates/monochange_core/changelog.md",
		"crates/monochange_dart/changelog.md",
		"crates/monochange_deno/changelog.md",
		"crates/monochange_ecmascript/changelog.md",
		"crates/monochange_forgejo/changelog.md",
		"crates/monochange_gitea/changelog.md",
		"crates/monochange_github/changelog.md",
		"crates/monochange_gitlab/changelog.md",
		"crates/monochange_go/changelog.md",
		"crates/monochange_graph/changelog.md",
		"crates/monochange_hosting/changelog.md",
		"crates/monochange_lint/changelog.md",
		"crates/monochange_linting/changelog.md",
		"crates/monochange_npm/changelog.md",
		"crates/monochange_publish/changelog.md",
		"crates/monochange_python/changelog.md",
		"crates/monochange_schema/changelog.md",
		"crates/monochange_semver/changelog.md",
		"crates/monochange_telemetry/changelog.md",
		"crates/monochange_test_helpers/changelog.md",
		"packages/monochange__cli/changelog.md",
		"packages/monochange__cli-darwin-arm64/changelog.md",
		"packages/monochange__cli-darwin-x64/changelog.md",
		"packages/monochange__cli-linux-arm64-gnu/changelog.md",
		"packages/monochange__cli-linux-arm64-musl/changelog.md",
		"packages/monochange__cli-linux-x64-gnu/changelog.md",
		"packages/monochange__cli-linux-x64-musl/changelog.md",
		"packages/monochange__cli-win32-arm64-msvc/changelog.md",
		"packages/monochange__cli-win32-x64-msvc/changelog.md",
		"packages/monochange__skill/changelog.md"
	],
	"deleted_changesets": [
		".changeset/add-reported-command-errors.md",
		".changeset/classify-rust-semver-matrix.md",
		".changeset/classify-typescript-declarations.md",
		".changeset/describe-built-in-step-inputs.md",
		".changeset/detect-deleted-packages.md",
		".changeset/docs-refresh-and-sample-tests.md",
		".changeset/fix-cli-output-contract.md",
		".changeset/fix-lint-autofix-manifest-wipe.md",
		".changeset/lint-rule-integration-fix-coverage.md",
		".changeset/make-cli-output-human-first.md",
		".changeset/make-publish-outcomes-obvious.md",
		".changeset/publish-readiness-verifies-trusted-publishing-and-order.md",
		".changeset/raise-default-publish-timeout.md",
		".changeset/release-aware-change-classification.md",
		".changeset/render-structured-release-notes.md",
		".changeset/stabilize-classification-git-errors.md",
		".changeset/unify-cli-progress-diagnostics.md",
		".changeset/update-agent-output-guidance.md",
		".changeset/update-deps-and-actions.md"
	],
	"changesets": [
		{
			"path": ".changeset/add-reported-command-errors.md",
			"summary": "Preserve structured command output when an operation fails",
			"details": "`MonochangeError::Reported` lets a command return a machine-readable result on stdout and a separate failure diagnostic. Use `reported_output()` at the process boundary to write the result before returning a non-zero exit status.\n\n```rust\nlet error = MonochangeError::Reported {\n\toutput: json_report,\n\tdiagnostic: \"check failed: 1 error\".to_string(),\n};\n\nassert_eq!(error.reported_output(), Some(expected_json));\n```\n\nCallers that only use `Display` or `render()` continue to receive the diagnostic without the attached output.",
			"targets": [
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a0752245832a61293285a5614e9886d1ba2b617d",
						"short_sha": "a075224",
						"url": "https://github.com/monochange/monochange/commit/a0752245832a61293285a5614e9886d1ba2b617d",
						"authored_at": "2026-09-08T12:26:57+01:00",
						"committed_at": "2026-09-08T13:05:33+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#661",
						"title": "fix(cli): enforce process output contracts",
						"url": "https://github.com/monochange/monochange/pull/661",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a0752245832a61293285a5614e9886d1ba2b617d",
						"short_sha": "a075224",
						"url": "https://github.com/monochange/monochange/commit/a0752245832a61293285a5614e9886d1ba2b617d",
						"authored_at": "2026-09-08T12:26:57+01:00",
						"committed_at": "2026-09-08T13:05:33+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#661",
						"title": "fix(cli): enforce process output contracts",
						"url": "https://github.com/monochange/monochange/pull/661",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/classify-rust-semver-matrix.md",
			"summary": "classify Rust compatibility across configured feature and target matrices",
			"details": "Cargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.",
			"targets": [
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_cargo",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "698fb27fc4b43ffca7c42c32334abded6011333e",
						"short_sha": "698fb27",
						"url": "https://github.com/monochange/monochange/commit/698fb27fc4b43ffca7c42c32334abded6011333e",
						"authored_at": "2026-09-09T11:52:21+01:00",
						"committed_at": "2026-09-09T12:08:00+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#673",
						"title": "feat: classify Rust compatibility matrices",
						"url": "https://github.com/monochange/monochange/pull/673",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "698fb27fc4b43ffca7c42c32334abded6011333e",
						"short_sha": "698fb27",
						"url": "https://github.com/monochange/monochange/commit/698fb27fc4b43ffca7c42c32334abded6011333e",
						"authored_at": "2026-09-09T11:52:21+01:00",
						"committed_at": "2026-09-09T12:08:00+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#673",
						"title": "feat: classify Rust compatibility matrices",
						"url": "https://github.com/monochange/monochange/pull/673",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/classify-typescript-declarations.md",
			"summary": "classify TypeScript declaration compatibility with the project compiler",
			"details": "`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.",
			"targets": [
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "monochange_cargo",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				},
				{
					"id": "monochange_dart",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_deno",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_ecmascript",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_npm",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_semver",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "1d44610604fb861e66b4d729ac5ab263a6cbdb82",
						"short_sha": "1d44610",
						"url": "https://github.com/monochange/monochange/commit/1d44610604fb861e66b4d729ac5ab263a6cbdb82",
						"authored_at": "2026-09-09T08:43:48+01:00",
						"committed_at": "2026-09-09T09:27:30+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#672",
						"title": "feat: classify TypeScript declaration compatibility",
						"url": "https://github.com/monochange/monochange/pull/672",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "1d44610604fb861e66b4d729ac5ab263a6cbdb82",
						"short_sha": "1d44610",
						"url": "https://github.com/monochange/monochange/commit/1d44610604fb861e66b4d729ac5ab263a6cbdb82",
						"authored_at": "2026-09-09T08:43:48+01:00",
						"committed_at": "2026-09-09T09:27:30+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#672",
						"title": "feat: classify TypeScript declaration compatibility",
						"url": "https://github.com/monochange/monochange/pull/672",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/describe-built-in-step-inputs.md",
			"summary": "Describe every built-in workflow step input",
			"details": "`CliStepDefinition::step_inputs_schema()` now provides non-empty `help_text` for every built-in input. CLIs and agent snapshots built from the schema can explain flags such as `--package`, `--show-all`, `--otp`, `--resume`, and `--sync-provider` without maintaining a second help-text table.\n\n```rust\nlet placeholder = monochange_core::all_step_variants()\n\t.into_iter()\n\t.find(|step| step.kind_name() == \"PlaceholderPublish\")\n\t.expect(\"placeholder step\");\n\nassert!(placeholder\n\t.step_inputs_schema()\n\t.iter()\n\t.all(|input| input.help_text.is_some()));\n```",
			"targets": [
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"short_sha": "a10bfa1",
						"url": "https://github.com/monochange/monochange/commit/a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"authored_at": "2026-09-08T14:26:35+01:00",
						"committed_at": "2026-09-08T22:03:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#662",
						"title": "feat(cli): default to human-readable output",
						"url": "https://github.com/monochange/monochange/pull/662",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"short_sha": "a10bfa1",
						"url": "https://github.com/monochange/monochange/commit/a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"authored_at": "2026-09-08T14:26:35+01:00",
						"committed_at": "2026-09-08T22:03:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#662",
						"title": "feat(cli): default to human-readable output",
						"url": "https://github.com/monochange/monochange/pull/662",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#661",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/661",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/detect-deleted-packages.md",
			"summary": "classify fully deleted packages as breaking changes",
			"details": "`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```",
			"targets": [
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_semver",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "d07107c1d4ddd6e7cbd2ce0b010db7c64855b5b0",
						"short_sha": "d07107c",
						"url": "https://github.com/monochange/monochange/commit/d07107c1d4ddd6e7cbd2ce0b010db7c64855b5b0",
						"authored_at": "2026-09-09T00:52:04+01:00",
						"committed_at": "2026-09-09T01:19:34+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#670",
						"title": "feat: classify fully deleted packages",
						"url": "https://github.com/monochange/monochange/pull/670",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "d07107c1d4ddd6e7cbd2ce0b010db7c64855b5b0",
						"short_sha": "d07107c",
						"url": "https://github.com/monochange/monochange/commit/d07107c1d4ddd6e7cbd2ce0b010db7c64855b5b0",
						"authored_at": "2026-09-09T00:52:04+01:00",
						"committed_at": "2026-09-09T01:19:34+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#670",
						"title": "feat: classify fully deleted packages",
						"url": "https://github.com/monochange/monochange/pull/670",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/docs-refresh-and-sample-tests.md",
			"summary": "refresh docs and validate doc samples in tests",
			"details": "Every crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.",
			"targets": [
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_cargo",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_dart",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_deno",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_forgejo",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_gitea",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_github",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_gitlab",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_go",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_graph",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_hosting",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_lint",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_linting",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_npm",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_python",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_semver",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_telemetry",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_test_helpers",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "3e4725fc41a731619fb50ef8dcaf9da98ecc454f",
						"short_sha": "3e4725f",
						"url": "https://github.com/monochange/monochange/commit/3e4725fc41a731619fb50ef8dcaf9da98ecc454f",
						"authored_at": "2026-09-06T23:34:11+01:00",
						"committed_at": "2026-09-06T23:34:11+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#656",
						"title": "docs: refresh stale docs, unify crate docs via mdt, validate samples in tests",
						"url": "https://github.com/monochange/monochange/pull/656",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "3e4725fc41a731619fb50ef8dcaf9da98ecc454f",
						"short_sha": "3e4725f",
						"url": "https://github.com/monochange/monochange/commit/3e4725fc41a731619fb50ef8dcaf9da98ecc454f",
						"authored_at": "2026-09-06T23:34:11+01:00",
						"committed_at": "2026-09-06T23:34:11+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#656",
						"title": "docs: refresh stale docs, unify crate docs via mdt, validate samples in tests",
						"url": "https://github.com/monochange/monochange/pull/656",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/fix-cli-output-contract.md",
			"summary": "Keep check failures and progress output safe for automation",
			"details": "`monochange check` now returns the same non-zero exit status for text, Markdown, JSON, and compact JSON when lint errors exist. JSON callers still receive the complete lint report on stdout.\n\n```bash\nmonochange check --format json\necho $?\n```\n\nBefore this change, a lint failure returned status `0` in JSON modes. It now returns status `1`, which lets CI stop on the failure without parsing `error_count` first.\n\nCaptured workflow progress no longer contains cursor-clearing escape sequences. `MONOCHANGE_NO_PROGRESS=1` also suppresses lint progress as documented, including explicit progress formats.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a0752245832a61293285a5614e9886d1ba2b617d",
						"short_sha": "a075224",
						"url": "https://github.com/monochange/monochange/commit/a0752245832a61293285a5614e9886d1ba2b617d",
						"authored_at": "2026-09-08T12:26:57+01:00",
						"committed_at": "2026-09-08T13:05:33+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#661",
						"title": "fix(cli): enforce process output contracts",
						"url": "https://github.com/monochange/monochange/pull/661",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a0752245832a61293285a5614e9886d1ba2b617d",
						"short_sha": "a075224",
						"url": "https://github.com/monochange/monochange/commit/a0752245832a61293285a5614e9886d1ba2b617d",
						"authored_at": "2026-09-08T12:26:57+01:00",
						"committed_at": "2026-09-08T13:05:33+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#661",
						"title": "fix(cli): enforce process output contracts",
						"url": "https://github.com/monochange/monochange/pull/661",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/fix-lint-autofix-manifest-wipe.md",
			"summary": "`monochange check --fix` no longer replaces whole manifests with a single line",
			"details": "Running `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.",
			"targets": [
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_lint",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_cargo",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_dart",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_npm",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "056fd62dd7724500df5bd54429e8c5bd6f3b777d",
						"short_sha": "056fd62",
						"url": "https://github.com/monochange/monochange/commit/056fd62dd7724500df5bd54429e8c5bd6f3b777d",
						"authored_at": "2026-09-08T09:49:02+01:00",
						"committed_at": "2026-09-08T09:49:02+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#658",
						"title": "fix(lint): stop autofixes from replacing manifests with fragments",
						"url": "https://github.com/monochange/monochange/pull/658",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "056fd62dd7724500df5bd54429e8c5bd6f3b777d",
						"short_sha": "056fd62",
						"url": "https://github.com/monochange/monochange/commit/056fd62dd7724500df5bd54429e8c5bd6f3b777d",
						"authored_at": "2026-09-08T09:49:02+01:00",
						"committed_at": "2026-09-08T09:49:02+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#658",
						"title": "fix(lint): stop autofixes from replacing manifests with fragments",
						"url": "https://github.com/monochange/monochange/pull/658",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/lint-rule-integration-fix-coverage.md",
			"summary": "Lock every lint rule's autofix behavior behind integration tests",
			"details": "Every lint rule now has integration coverage that runs `monochange check --fix` against file fixtures and snapshots the fixed manifests, so a rule that deletes or corrupts manifest content can no longer land unnoticed. The fixtures cover all cargo, npm, dart, and changeset rules, including the rules without autofixes whose diagnostics must persist.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5e4fcc707736288634e6108d9ae57d0f677b6356",
						"short_sha": "5e4fcc7",
						"url": "https://github.com/monochange/monochange/commit/5e4fcc707736288634e6108d9ae57d0f677b6356",
						"authored_at": "2026-09-08T10:47:20Z",
						"committed_at": "2026-09-08T10:47:20Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#659",
						"title": "test(lint): add fix-behavior integration tests for every lint rule",
						"url": "https://github.com/monochange/monochange/pull/659",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5e4fcc707736288634e6108d9ae57d0f677b6356",
						"short_sha": "5e4fcc7",
						"url": "https://github.com/monochange/monochange/commit/5e4fcc707736288634e6108d9ae57d0f677b6356",
						"authored_at": "2026-09-08T10:47:20Z",
						"committed_at": "2026-09-08T10:47:20Z",
						"author_name": "Ifiok Jr.",
						"author_email": "1160934+ifiokjr@users.noreply.github.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#659",
						"title": "test(lint): add fix-behavior integration tests for every lint rule",
						"url": "https://github.com/monochange/monochange/pull/659",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#658",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/658",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/make-cli-output-human-first.md",
			"summary": "Make human-readable text the default CLI result",
			"details": "monochange commands now print concise text unless you explicitly request Markdown or JSON. `monochange step config` prints a short workspace summary instead of the complete resolved configuration.\n\n```bash\n# human summary\nmonochange step config\n\n# complete structured configuration\nmonochange step config --format json\n```\n\nExplicit `--format markdown` output stays as raw Markdown even when stdout is a terminal. `--jq` now requires `--format json` or `--format json-min`, so it cannot run a mutating command and only then discover that the result was not JSON.\n\n`--quiet` now controls output only. It no longer silently changes a real operation into a dry run. Add `--dry-run` explicitly when you need both behaviors:\n\n```bash\nmonochange run release --dry-run --quiet\n```\n\nConfigured workflows that intentionally set their own `format` default keep that explicit choice.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"short_sha": "a10bfa1",
						"url": "https://github.com/monochange/monochange/commit/a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"authored_at": "2026-09-08T14:26:35+01:00",
						"committed_at": "2026-09-08T22:03:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#662",
						"title": "feat(cli): default to human-readable output",
						"url": "https://github.com/monochange/monochange/pull/662",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"short_sha": "a10bfa1",
						"url": "https://github.com/monochange/monochange/commit/a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"authored_at": "2026-09-08T14:26:35+01:00",
						"committed_at": "2026-09-08T22:03:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#662",
						"title": "feat(cli): default to human-readable output",
						"url": "https://github.com/monochange/monochange/pull/662",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#661",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/661",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/make-publish-outcomes-obvious.md",
			"summary": "Make package publishing outcomes obvious",
			"details": "Package publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_publish",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "breaking"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "67dbad18a37038239301ff43b9d61342d889f3e3",
						"short_sha": "67dbad1",
						"url": "https://github.com/monochange/monochange/commit/67dbad18a37038239301ff43b9d61342d889f3e3",
						"authored_at": "2026-09-08T15:37:46+01:00",
						"committed_at": "2026-09-08T23:39:03+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#663",
						"title": "feat(cli): make publish outcomes obvious",
						"url": "https://github.com/monochange/monochange/pull/663",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "67dbad18a37038239301ff43b9d61342d889f3e3",
						"short_sha": "67dbad1",
						"url": "https://github.com/monochange/monochange/commit/67dbad18a37038239301ff43b9d61342d889f3e3",
						"authored_at": "2026-09-08T15:37:46+01:00",
						"committed_at": "2026-09-08T23:39:03+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#663",
						"title": "feat(cli): make publish outcomes obvious",
						"url": "https://github.com/monochange/monochange/pull/663",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/publish-readiness-verifies-trusted-publishing-and-order.md",
			"summary": "publish readiness verifies trusted publishing and publish order",
			"details": "`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_publish",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "4e1c01fc6cbf514ddd41008fa979c138789eb1cf",
						"short_sha": "4e1c01f",
						"url": "https://github.com/monochange/monochange/commit/4e1c01fc6cbf514ddd41008fa979c138789eb1cf",
						"authored_at": "2026-09-08T13:11:46+01:00",
						"committed_at": "2026-09-09T18:15:52+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#666",
						"title": "feat(publish): verify trusted publishing and publish order in readiness",
						"url": "https://github.com/monochange/monochange/pull/666",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "c7d37d0b85a0fed21ffd853fd5dffb9cd1c8ea16",
						"short_sha": "c7d37d0",
						"url": "https://github.com/monochange/monochange/commit/c7d37d0b85a0fed21ffd853fd5dffb9cd1c8ea16",
						"authored_at": "2026-09-09T18:46:55+01:00",
						"committed_at": "2026-09-09T18:46:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#666",
						"title": "feat(publish): verify trusted publishing and publish order in readiness",
						"url": "https://github.com/monochange/monochange/pull/666",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/raise-default-publish-timeout.md",
			"summary": "Raise the default publish timeout from 60 to 300 seconds",
			"details": "`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```",
			"targets": [
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_schema",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "major"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "eac63d19975cfa7e7f450c280b0d9a7fde044b5b",
						"short_sha": "eac63d1",
						"url": "https://github.com/monochange/monochange/commit/eac63d19975cfa7e7f450c280b0d9a7fde044b5b",
						"authored_at": "2026-09-09T06:14:04+01:00",
						"committed_at": "2026-09-09T06:14:04+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#671",
						"title": "fix(publish): raise the default publish timeout from 60 to 600 seconds",
						"url": "https://github.com/monochange/monochange/pull/671",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "b77a313b5a14657dea996d612d4c5b3ddf879393",
						"short_sha": "b77a313",
						"url": "https://github.com/monochange/monochange/commit/b77a313b5a14657dea996d612d4c5b3ddf879393",
						"authored_at": "2026-09-09T10:17:03+01:00",
						"committed_at": "2026-09-09T10:17:03+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#671",
						"title": "fix(publish): raise the default publish timeout from 60 to 600 seconds",
						"url": "https://github.com/monochange/monochange/pull/671",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#630",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/630",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/release-aware-change-classification.md",
			"summary": "classify pull request severity against both the default branch and latest release",
			"details": "Agents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.",
			"targets": [
				{
					"id": "@monochange/cli",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange",
					"kind": "package",
					"bump": "major",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "major"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "minor"
				},
				{
					"id": "monochange_ecmascript",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "d58d8b1c1ebf53953e95964b8c2bf31fe8999ec4",
						"short_sha": "d58d8b1",
						"url": "https://github.com/monochange/monochange/commit/d58d8b1c1ebf53953e95964b8c2bf31fe8999ec4",
						"authored_at": "2026-09-08T16:02:14+01:00",
						"committed_at": "2026-09-08T16:02:14+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#664",
						"title": "feat: classify release-aware change severity",
						"url": "https://github.com/monochange/monochange/pull/664",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "53579a9cd582d98687eee512e26d967c1a68e42b",
						"short_sha": "53579a9",
						"url": "https://github.com/monochange/monochange/commit/53579a9cd582d98687eee512e26d967c1a68e42b",
						"authored_at": "2026-09-08T17:19:18+01:00",
						"committed_at": "2026-09-08T17:19:18+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#664",
						"title": "feat: classify release-aware change severity",
						"url": "https://github.com/monochange/monochange/pull/664",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/render-structured-release-notes.md",
			"summary": "Keep release notes structured until their output format is known",
			"details": "JSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "monochange_changelog",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "monochange_config",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				},
				{
					"id": "monochange_core",
					"kind": "package",
					"bump": "minor",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "feat"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "42b364e646f17fe551c12c0f1415ed3b59b2a137",
						"short_sha": "42b364e",
						"url": "https://github.com/monochange/monochange/commit/42b364e646f17fe551c12c0f1415ed3b59b2a137",
						"authored_at": "2026-09-08T20:32:36+01:00",
						"committed_at": "2026-09-09T06:57:26+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#668",
						"title": "feat(changelog): render structured release notes",
						"url": "https://github.com/monochange/monochange/pull/668",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "42b364e646f17fe551c12c0f1415ed3b59b2a137",
						"short_sha": "42b364e",
						"url": "https://github.com/monochange/monochange/commit/42b364e646f17fe551c12c0f1415ed3b59b2a137",
						"authored_at": "2026-09-08T20:32:36+01:00",
						"committed_at": "2026-09-09T06:57:26+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#668",
						"title": "feat(changelog): render structured release notes",
						"url": "https://github.com/monochange/monochange/pull/668",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/stabilize-classification-git-errors.md",
			"summary": "preserve git failure diagnostics during batched revision reads",
			"details": "`monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.",
			"targets": [
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				},
				{
					"id": "monochange_analysis",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "6fce0f34c8f18cee066425982b500d5e2848d480",
						"short_sha": "6fce0f3",
						"url": "https://github.com/monochange/monochange/commit/6fce0f34c8f18cee066425982b500d5e2848d480",
						"authored_at": "2026-09-08T18:12:16+01:00",
						"committed_at": "2026-09-08T18:12:16+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#665",
						"title": "fix: stabilize release classification diagnostics",
						"url": "https://github.com/monochange/monochange/pull/665",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "5fa8dd944e517214d5a17a6979712fc70aacf449",
						"short_sha": "5fa8dd9",
						"url": "https://github.com/monochange/monochange/commit/5fa8dd944e517214d5a17a6979712fc70aacf449",
						"authored_at": "2026-09-08T18:20:17+01:00",
						"committed_at": "2026-09-08T18:20:17+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#665",
						"title": "fix: stabilize release classification diagnostics",
						"url": "https://github.com/monochange/monochange/pull/665",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#664",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/664",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/unify-cli-progress-diagnostics.md",
			"summary": "Explain command progress and failures without debug logging",
			"details": "monochange now uses one progress reporter for workspace loading, validation, linting, publishing, workflow steps, and subprocess output. Interactive terminals can animate, while CI and captured output receive the same events as complete lines without terminal control sequences.\n\nFailures now include a stable diagnostic code, relevant command or file context, and a suggested next action when one is available. Failed checks keep their full result on stdout, so automation can inspect it without searching through stderr.\n\nUse `--progress-format json` for a newline-delimited machine event stream. `--log-level debug` remains available for maintainer tracing and now disables animation so trace records stay readable.",
			"targets": [
				{
					"id": "monochange",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "c2fad72af07791184bf8a2c27b8a549175a4c068",
						"short_sha": "c2fad72",
						"url": "https://github.com/monochange/monochange/commit/c2fad72af07791184bf8a2c27b8a549175a4c068",
						"authored_at": "2026-09-08T18:24:00+01:00",
						"committed_at": "2026-09-09T06:21:06+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#667",
						"title": "feat(cli): unify progress and diagnostics",
						"url": "https://github.com/monochange/monochange/pull/667",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "c2fad72af07791184bf8a2c27b8a549175a4c068",
						"short_sha": "c2fad72",
						"url": "https://github.com/monochange/monochange/commit/c2fad72af07791184bf8a2c27b8a549175a4c068",
						"authored_at": "2026-09-08T18:24:00+01:00",
						"committed_at": "2026-09-09T06:21:06+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#667",
						"title": "feat(cli): unify progress and diagnostics",
						"url": "https://github.com/monochange/monochange/pull/667",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		},
		{
			"path": ".changeset/update-agent-output-guidance.md",
			"summary": "Teach agents to choose structured output explicitly",
			"details": "The monochange skill examples now use text for human-facing command defaults and request JSON or Markdown only when the workflow needs that artifact format. The guidance also treats `--quiet` and `--dry-run` as independent choices, matching the CLI contract.",
			"targets": [
				{
					"id": "@monochange/skill",
					"kind": "package",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"short_sha": "a10bfa1",
						"url": "https://github.com/monochange/monochange/commit/a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"authored_at": "2026-09-08T14:26:35+01:00",
						"committed_at": "2026-09-08T22:03:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#662",
						"title": "feat(cli): default to human-readable output",
						"url": "https://github.com/monochange/monochange/pull/662",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"short_sha": "a10bfa1",
						"url": "https://github.com/monochange/monochange/commit/a10bfa1b087c9ded1940d62c3b46aa8b3e3e179a",
						"authored_at": "2026-09-08T14:26:35+01:00",
						"committed_at": "2026-09-08T22:03:55+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#662",
						"title": "feat(cli): default to human-readable output",
						"url": "https://github.com/monochange/monochange/pull/662",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": [
					{
						"provider": "github",
						"host": "github.com",
						"id": "#661",
						"title": null,
						"url": "https://github.com/monochange/monochange/issues/661",
						"relationship": "referenced_by_review_request"
					}
				]
			}
		},
		{
			"path": ".changeset/update-deps-and-actions.md",
			"summary": "update dependencies and GitHub Actions",
			"details": "Direct crate dependencies were bumped to their latest versions: `octocrab` 0.54.1, `oxc_allocator`/`oxc_ast`/`oxc_parser`/`oxc_span` 0.149.0 (with the parser migration to the split `ExportDeclaration`/`ExportNamedDeclaration`/`ExportFromDeclaration` module declarations and `TSNamespaceDeclaration`/`TSExternalModuleDeclaration` replacing `TSModuleDeclaration`), `rmcp` 3.2 (tool results now use `ContentBlock` instead of the removed `Content` wrapper), `syn` 3.0.5, `jsonschema` 0.55, `termimad` 0.35, `shlex` 2, `rstest` 0.27, `insta-cmd` 0.7, and `time` 0.3.55. The `time` crate's exact-version pin (`=0.3.47`) was removed because the octocrab `From` impl coherence failure it guarded against no longer reproduces on octocrab 0.54; `cargo deny` now allows the `Zlib` license (`foldhash` via `jsonschema`'s `referencing`) and tracks the new duplicate `base64`/`num-bigint`/`syn` versions.\n\nNo public API changed; the crate manifest dependency requirements move with this patch release. GitHub Actions were also refreshed to their latest releases (checkout v7.0.1, upload-artifact v7.0.1, download-artifact v8.0.1, cache v6.1.0, setup-node v7.0.0, pages actions v5/v6, attest-build-provenance v4.2.2, zizmor-action v0.6.3, changed-files v47.0.6, rust-cache v2.9.2, install-nix-action v31.11.1, install-action v2.87.8, setup-cross-toolchain v1.42.0, crates-io-auth-action v1.0.5, pnpm/action-setup v6.1.0, and the dtolnay/rust-toolchain branch pins), all still pinned by commit hash.",
			"targets": [
				{
					"id": "main",
					"kind": "group",
					"bump": "patch",
					"origin": "direct-change",
					"evidence_refs": [],
					"change_type": "patch"
				}
			],
			"context": {
				"provider": "github",
				"host": "github.com",
				"capabilities": {
					"commit_web_urls": true,
					"actor_profiles": true,
					"review_request_lookup": true,
					"related_issues": true,
					"issue_comments": true
				},
				"introduced": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "7feed524c3bcf8bbb5144eee737bc691ae25a4ab",
						"short_sha": "7feed52",
						"url": "https://github.com/monochange/monochange/commit/7feed524c3bcf8bbb5144eee737bc691ae25a4ab",
						"authored_at": "2026-09-08T12:03:48+01:00",
						"committed_at": "2026-09-08T12:03:48+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#660",
						"title": "chore: update dependencies and GitHub Actions to latest versions",
						"url": "https://github.com/monochange/monochange/pull/660",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"last_updated": {
					"actor": {
						"provider": "github",
						"host": "github.com",
						"id": null,
						"login": "ifiokjr",
						"display_name": "ifiokjr",
						"url": "https://github.com/ifiokjr",
						"source": "review_request_author"
					},
					"commit": {
						"provider": "github",
						"host": "github.com",
						"sha": "7feed524c3bcf8bbb5144eee737bc691ae25a4ab",
						"short_sha": "7feed52",
						"url": "https://github.com/monochange/monochange/commit/7feed524c3bcf8bbb5144eee737bc691ae25a4ab",
						"authored_at": "2026-09-08T12:03:48+01:00",
						"committed_at": "2026-09-08T12:03:48+01:00",
						"author_name": "Ifiok Jr.",
						"author_email": "ifiokotung@gmail.com"
					},
					"review_request": {
						"provider": "github",
						"host": "github.com",
						"kind": "pull_request",
						"id": "#660",
						"title": "chore: update dependencies and GitHub Actions to latest versions",
						"url": "https://github.com/monochange/monochange/pull/660",
						"author": {
							"provider": "github",
							"host": "github.com",
							"id": null,
							"login": "ifiokjr",
							"display_name": "ifiokjr",
							"url": "https://github.com/ifiokjr",
							"source": "review_request_author"
						}
					}
				},
				"related_issues": []
			}
		}
	],
	"changelogs": [
		{
			"owner_id": "main",
			"owner_kind": "group",
			"output": "default",
			"stream": "default",
			"path": "changelog.md",
			"format": "monochange",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [
					"Grouped release for `main`."
				],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### classify pull request severity against both the default branch and latest release\n\n_Packages:_ _monochange_\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n_Packages:_ _monochange_analysis_, _monochange_core_\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify Rust compatibility across configured feature and target matrices\n\n_Packages:_ _monochange_cargo_, _monochange_core_\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### Make package publishing outcomes obvious\n\n_Packages:_ _monochange_publish_\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)"
						]
					},
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_, _monochange_analysis_, _monochange_config_\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_, _monochange_npm_, _monochange_semver_\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify pull request severity against both the default branch and latest release\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_analysis_, _monochange_core_\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)",
							"#### classify fully deleted packages as breaking changes\n\n_Packages:_ _@monochange/skill_, _monochange_, _monochange_analysis_, _monochange_core_, _monochange_semver_\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)",
							"#### publish readiness verifies trusted publishing and publish order\n\n_Packages:_ _monochange_, _monochange_publish_\n\n`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #666](https://github.com/monochange/monochange/pull/666)",
							"#### Keep release notes structured until their output format is known\n\n_Packages:_ _monochange_, _monochange_changelog_, _monochange_config_, _monochange_core_\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_, _monochange_analysis_, _monochange_cargo_, _monochange_config_, _monochange_core_, _monochange_dart_, _monochange_deno_, _monochange_forgejo_, _monochange_gitea_, _monochange_github_, _monochange_gitlab_, _monochange_go_, _monochange_graph_, _monochange_hosting_, _monochange_lint_, _monochange_linting_, _monochange_npm_, _monochange_python_, _monochange_semver_, _monochange_telemetry_, _monochange_test_helpers_\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\n_Packages:_ _@monochange/cli_, _monochange_, _monochange_cargo_, _monochange_core_, _monochange_dart_, _monochange_lint_, _monochange_npm_\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)",
							"#### Raise the default publish timeout from 60 to 300 seconds\n\n_Packages:_ _@monochange/cli_, _monochange_, _monochange_config_, _monochange_core_\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)",
							"#### update dependencies and GitHub Actions\n\nDirect crate dependencies were bumped to their latest versions: `octocrab` 0.54.1, `oxc_allocator`/`oxc_ast`/`oxc_parser`/`oxc_span` 0.149.0 (with the parser migration to the split `ExportDeclaration`/`ExportNamedDeclaration`/`ExportFromDeclaration` module declarations and `TSNamespaceDeclaration`/`TSExternalModuleDeclaration` replacing `TSModuleDeclaration`), `rmcp` 3.2 (tool results now use `ContentBlock` instead of the removed `Content` wrapper), `syn` 3.0.5, `jsonschema` 0.55, `termimad` 0.35, `shlex` 2, `rstest` 0.27, `insta-cmd` 0.7, and `time` 0.3.55. The `time` crate's exact-version pin (`=0.3.47`) was removed because the octocrab `From` impl coherence failure it guarded against no longer reproduces on octocrab 0.54; `cargo deny` now allows the `Zlib` license (`foldhash` via `jsonschema`'s `referencing`) and tracks the new duplicate `base64`/`num-bigint`/`syn` versions.\n\nNo public API changed; the crate manifest dependency requirements move with this patch release. GitHub Actions were also refreshed to their latest releases (checkout v7.0.1, upload-artifact v7.0.1, download-artifact v8.0.1, cache v6.1.0, setup-node v7.0.0, pages actions v5/v6, attest-build-provenance v4.2.2, zizmor-action v0.6.3, changed-files v47.0.6, rust-cache v2.9.2, install-nix-action v31.11.1, install-action v2.87.8, setup-cross-toolchain v1.42.0, crates-io-auth-action v1.0.5, pnpm/action-setup v6.1.0, and the dtolnay/rust-toolchain branch pins), all still pinned by commit hash.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #660](https://github.com/monochange/monochange/pull/660)",
							"- _Packages:_ _@monochange/skill_, _monochange_analysis_**preserve git failure diagnostics during batched revision reads.** `monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #665](https://github.com/monochange/monochange/pull/665) · _Related issues:_ [#664](https://github.com/monochange/monochange/issues/664)",
							"- **@monochange/skill**: **Teach agents to choose structured output explicitly.** The monochange skill examples now use text for human-facing command defaults and request JSON or Markdown only when the workflow needs that artifact format. The guidance also treats `--quiet` and `--dry-run` as independent choices, matching the CLI contract.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)",
							"#### Keep check failures and progress output safe for automation\n\n_Packages:_ _monochange_\n\n`monochange check` now returns the same non-zero exit status for text, Markdown, JSON, and compact JSON when lint errors exist. JSON callers still receive the complete lint report on stdout.\n\n```bash\nmonochange check --format json\necho $?\n```\n\nBefore this change, a lint failure returned status `0` in JSON modes. It now returns status `1`, which lets CI stop on the failure without parsing `error_count` first.\n\nCaptured workflow progress no longer contains cursor-clearing escape sequences. `MONOCHANGE_NO_PROGRESS=1` also suppresses lint progress as documented, including explicit progress formats.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)",
							"- **monochange**: **Lock every lint rule's autofix behavior behind integration tests.** Every lint rule now has integration coverage that runs `monochange check --fix` against file fixtures and snapshots the fixed manifests, so a rule that deletes or corrupts manifest content can no longer land unnoticed. The fixtures cover all cargo, npm, dart, and changeset rules, including the rules without autofixes whose diagnostics must persist.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #659](https://github.com/monochange/monochange/pull/659) · _Related issues:_ [#658](https://github.com/monochange/monochange/issues/658)",
							"#### Make human-readable text the default CLI result\n\n_Packages:_ _monochange_\n\nmonochange commands now print concise text unless you explicitly request Markdown or JSON. `monochange step config` prints a short workspace summary instead of the complete resolved configuration.\n\n```bash\n# human summary\nmonochange step config\n\n# complete structured configuration\nmonochange step config --format json\n```\n\nExplicit `--format markdown` output stays as raw Markdown even when stdout is a terminal. `--jq` now requires `--format json` or `--format json-min`, so it cannot run a mutating command and only then discover that the result was not JSON.\n\n`--quiet` now controls output only. It no longer silently changes a real operation into a dry run. Add `--dry-run` explicitly when you need both behaviors:\n\n```bash\nmonochange run release --dry-run --quiet\n```\n\nConfigured workflows that intentionally set their own `format` default keep that explicit choice.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)",
							"#### Make package publishing outcomes obvious\n\n_Packages:_ _monochange_, _monochange_core_\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)",
							"#### Explain command progress and failures without debug logging\n\n_Packages:_ _monochange_\n\nmonochange now uses one progress reporter for workspace loading, validation, linting, publishing, workflow steps, and subprocess output. Interactive terminals can animate, while CI and captured output receive the same events as complete lines without terminal control sequences.\n\nFailures now include a stable diagnostic code, relevant command or file context, and a suggested next action when one is available. Failed checks keep their full result on stdout, so automation can inspect it without searching through stderr.\n\nUse `--progress-format json` for a newline-delimited machine event stream. `--log-level debug` remains available for maintainer tracing and now disables animation so trace records stay readable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #667](https://github.com/monochange/monochange/pull/667)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n_Packages:_ _monochange_cargo_, _monochange_dart_, _monochange_deno_, _monochange_ecmascript_\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### Preserve structured command output when an operation fails\n\n_Packages:_ _monochange_core_\n\n`MonochangeError::Reported` lets a command return a machine-readable result on stdout and a separate failure diagnostic. Use `reported_output()` at the process boundary to write the result before returning a non-zero exit status.\n\n```rust\nlet error = MonochangeError::Reported {\n\toutput: json_report,\n\tdiagnostic: \"check failed: 1 error\".to_string(),\n};\n\nassert_eq!(error.reported_output(), Some(expected_json));\n```\n\nCallers that only use `Display` or `render()` continue to receive the diagnostic without the attached output.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)",
							"#### Describe every built-in workflow step input\n\n_Packages:_ _monochange_core_\n\n`CliStepDefinition::step_inputs_schema()` now provides non-empty `help_text` for every built-in input. CLIs and agent snapshots built from the schema can explain flags such as `--package`, `--show-all`, `--otp`, `--resume`, and `--sync-provider` without maintaining a second help-text table.\n\n```rust\nlet placeholder = monochange_core::all_step_variants()\n\t.into_iter()\n\t.find(|step| step.kind_name() == \"PlaceholderPublish\")\n\t.expect(\"placeholder step\");\n\nassert!(placeholder\n\t.step_inputs_schema()\n\t.iter()\n\t.all(|input| input.help_text.is_some()));\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)",
							"#### classify pull request severity against both the default branch and latest release\n\n_Packages:_ _monochange_ecmascript_\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\nGrouped release for `main`.\n\n### 💥 Breaking Change\n\n#### classify pull request severity against both the default branch and latest release\n\n_Packages:_ _monochange_\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n_Packages:_ _monochange_analysis_, _monochange_core_\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify Rust compatibility across configured feature and target matrices\n\n_Packages:_ _monochange_cargo_, _monochange_core_\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### Make package publishing outcomes obvious\n\n_Packages:_ _monochange_publish_\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_, _monochange_analysis_, _monochange_config_\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_, _monochange_npm_, _monochange_semver_\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify pull request severity against both the default branch and latest release\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_analysis_, _monochange_core_\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n#### classify fully deleted packages as breaking changes\n\n_Packages:_ _@monochange/skill_, _monochange_, _monochange_analysis_, _monochange_core_, _monochange_semver_\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)\n\n#### publish readiness verifies trusted publishing and publish order\n\n_Packages:_ _monochange_, _monochange_publish_\n\n`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #666](https://github.com/monochange/monochange/pull/666)\n\n#### Keep release notes structured until their output format is known\n\n_Packages:_ _monochange_, _monochange_changelog_, _monochange_config_, _monochange_core_\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\n_Packages:_ _@monochange/cli_, _@monochange/skill_, _monochange_, _monochange_analysis_, _monochange_cargo_, _monochange_config_, _monochange_core_, _monochange_dart_, _monochange_deno_, _monochange_forgejo_, _monochange_gitea_, _monochange_github_, _monochange_gitlab_, _monochange_go_, _monochange_graph_, _monochange_hosting_, _monochange_lint_, _monochange_linting_, _monochange_npm_, _monochange_python_, _monochange_semver_, _monochange_telemetry_, _monochange_test_helpers_\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\n_Packages:_ _@monochange/cli_, _monochange_, _monochange_cargo_, _monochange_core_, _monochange_dart_, _monochange_lint_, _monochange_npm_\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)\n\n#### Raise the default publish timeout from 60 to 300 seconds\n\n_Packages:_ _@monochange/cli_, _monochange_, _monochange_config_, _monochange_core_\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)\n\n#### update dependencies and GitHub Actions\n\nDirect crate dependencies were bumped to their latest versions: `octocrab` 0.54.1, `oxc_allocator`/`oxc_ast`/`oxc_parser`/`oxc_span` 0.149.0 (with the parser migration to the split `ExportDeclaration`/`ExportNamedDeclaration`/`ExportFromDeclaration` module declarations and `TSNamespaceDeclaration`/`TSExternalModuleDeclaration` replacing `TSModuleDeclaration`), `rmcp` 3.2 (tool results now use `ContentBlock` instead of the removed `Content` wrapper), `syn` 3.0.5, `jsonschema` 0.55, `termimad` 0.35, `shlex` 2, `rstest` 0.27, `insta-cmd` 0.7, and `time` 0.3.55. The `time` crate's exact-version pin (`=0.3.47`) was removed because the octocrab `From` impl coherence failure it guarded against no longer reproduces on octocrab 0.54; `cargo deny` now allows the `Zlib` license (`foldhash` via `jsonschema`'s `referencing`) and tracks the new duplicate `base64`/`num-bigint`/`syn` versions.\n\nNo public API changed; the crate manifest dependency requirements move with this patch release. GitHub Actions were also refreshed to their latest releases (checkout v7.0.1, upload-artifact v7.0.1, download-artifact v8.0.1, cache v6.1.0, setup-node v7.0.0, pages actions v5/v6, attest-build-provenance v4.2.2, zizmor-action v0.6.3, changed-files v47.0.6, rust-cache v2.9.2, install-nix-action v31.11.1, install-action v2.87.8, setup-cross-toolchain v1.42.0, crates-io-auth-action v1.0.5, pnpm/action-setup v6.1.0, and the dtolnay/rust-toolchain branch pins), all still pinned by commit hash.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #660](https://github.com/monochange/monochange/pull/660)\n\n- _Packages:_ _@monochange/skill_, _monochange_analysis_**preserve git failure diagnostics during batched revision reads.** `monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #665](https://github.com/monochange/monochange/pull/665) · _Related issues:_ [#664](https://github.com/monochange/monochange/issues/664)\n- **@monochange/skill**: **Teach agents to choose structured output explicitly.** The monochange skill examples now use text for human-facing command defaults and request JSON or Markdown only when the workflow needs that artifact format. The guidance also treats `--quiet` and `--dry-run` as independent choices, matching the CLI contract.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)\n#### Keep check failures and progress output safe for automation\n\n_Packages:_ _monochange_\n\n`monochange check` now returns the same non-zero exit status for text, Markdown, JSON, and compact JSON when lint errors exist. JSON callers still receive the complete lint report on stdout.\n\n```bash\nmonochange check --format json\necho $?\n```\n\nBefore this change, a lint failure returned status `0` in JSON modes. It now returns status `1`, which lets CI stop on the failure without parsing `error_count` first.\n\nCaptured workflow progress no longer contains cursor-clearing escape sequences. `MONOCHANGE_NO_PROGRESS=1` also suppresses lint progress as documented, including explicit progress formats.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)\n\n- **monochange**: **Lock every lint rule's autofix behavior behind integration tests.** Every lint rule now has integration coverage that runs `monochange check --fix` against file fixtures and snapshots the fixed manifests, so a rule that deletes or corrupts manifest content can no longer land unnoticed. The fixtures cover all cargo, npm, dart, and changeset rules, including the rules without autofixes whose diagnostics must persist.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #659](https://github.com/monochange/monochange/pull/659) · _Related issues:_ [#658](https://github.com/monochange/monochange/issues/658)\n#### Make human-readable text the default CLI result\n\n_Packages:_ _monochange_\n\nmonochange commands now print concise text unless you explicitly request Markdown or JSON. `monochange step config` prints a short workspace summary instead of the complete resolved configuration.\n\n```bash\n# human summary\nmonochange step config\n\n# complete structured configuration\nmonochange step config --format json\n```\n\nExplicit `--format markdown` output stays as raw Markdown even when stdout is a terminal. `--jq` now requires `--format json` or `--format json-min`, so it cannot run a mutating command and only then discover that the result was not JSON.\n\n`--quiet` now controls output only. It no longer silently changes a real operation into a dry run. Add `--dry-run` explicitly when you need both behaviors:\n\n```bash\nmonochange run release --dry-run --quiet\n```\n\nConfigured workflows that intentionally set their own `format` default keep that explicit choice.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)\n\n#### Make package publishing outcomes obvious\n\n_Packages:_ _monochange_, _monochange_core_\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)\n\n#### Explain command progress and failures without debug logging\n\n_Packages:_ _monochange_\n\nmonochange now uses one progress reporter for workspace loading, validation, linting, publishing, workflow steps, and subprocess output. Interactive terminals can animate, while CI and captured output receive the same events as complete lines without terminal control sequences.\n\nFailures now include a stable diagnostic code, relevant command or file context, and a suggested next action when one is available. Failed checks keep their full result on stdout, so automation can inspect it without searching through stderr.\n\nUse `--progress-format json` for a newline-delimited machine event stream. `--log-level debug` remains available for maintainer tracing and now disables animation so trace records stay readable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #667](https://github.com/monochange/monochange/pull/667)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n_Packages:_ _monochange_cargo_, _monochange_dart_, _monochange_deno_, _monochange_ecmascript_\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### Preserve structured command output when an operation fails\n\n_Packages:_ _monochange_core_\n\n`MonochangeError::Reported` lets a command return a machine-readable result on stdout and a separate failure diagnostic. Use `reported_output()` at the process boundary to write the result before returning a non-zero exit status.\n\n```rust\nlet error = MonochangeError::Reported {\n\toutput: json_report,\n\tdiagnostic: \"check failed: 1 error\".to_string(),\n};\n\nassert_eq!(error.reported_output(), Some(expected_json));\n```\n\nCallers that only use `Display` or `render()` continue to receive the diagnostic without the attached output.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)\n\n#### Describe every built-in workflow step input\n\n_Packages:_ _monochange_core_\n\n`CliStepDefinition::step_inputs_schema()` now provides non-empty `help_text` for every built-in input. CLIs and agent snapshots built from the schema can explain flags such as `--package`, `--show-all`, `--otp`, `--resume`, and `--sync-provider` without maintaining a second help-text table.\n\n```rust\nlet placeholder = monochange_core::all_step_variants()\n\t.into_iter()\n\t.find(|step| step.kind_name() == \"PlaceholderPublish\")\n\t.expect(\"placeholder step\");\n\nassert!(placeholder\n\t.step_inputs_schema()\n\t.iter()\n\t.all(|input| input.help_text.is_some()));\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)\n\n#### classify pull request severity against both the default branch and latest release\n\n_Packages:_ _monochange_ecmascript_\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
		},
		{
			"owner_id": "monochange",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
						]
					},
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)",
							"#### publish readiness verifies trusted publishing and publish order\n\n`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #666](https://github.com/monochange/monochange/pull/666)",
							"#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### Keep check failures and progress output safe for automation\n\n`monochange check` now returns the same non-zero exit status for text, Markdown, JSON, and compact JSON when lint errors exist. JSON callers still receive the complete lint report on stdout.\n\n```bash\nmonochange check --format json\necho $?\n```\n\nBefore this change, a lint failure returned status `0` in JSON modes. It now returns status `1`, which lets CI stop on the failure without parsing `error_count` first.\n\nCaptured workflow progress no longer contains cursor-clearing escape sequences. `MONOCHANGE_NO_PROGRESS=1` also suppresses lint progress as documented, including explicit progress formats.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)",
							"- **Lock every lint rule's autofix behavior behind integration tests.** Every lint rule now has integration coverage that runs `monochange check --fix` against file fixtures and snapshots the fixed manifests, so a rule that deletes or corrupts manifest content can no longer land unnoticed. The fixtures cover all cargo, npm, dart, and changeset rules, including the rules without autofixes whose diagnostics must persist.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #659](https://github.com/monochange/monochange/pull/659) · _Related issues:_ [#658](https://github.com/monochange/monochange/issues/658)",
							"#### Make human-readable text the default CLI result\n\nmonochange commands now print concise text unless you explicitly request Markdown or JSON. `monochange step config` prints a short workspace summary instead of the complete resolved configuration.\n\n```bash\n# human summary\nmonochange step config\n\n# complete structured configuration\nmonochange step config --format json\n```\n\nExplicit `--format markdown` output stays as raw Markdown even when stdout is a terminal. `--jq` now requires `--format json` or `--format json-min`, so it cannot run a mutating command and only then discover that the result was not JSON.\n\n`--quiet` now controls output only. It no longer silently changes a real operation into a dry run. Add `--dry-run` explicitly when you need both behaviors:\n\n```bash\nmonochange run release --dry-run --quiet\n```\n\nConfigured workflows that intentionally set their own `format` default keep that explicit choice.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)",
							"#### Make package publishing outcomes obvious\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)",
							"#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)",
							"#### Explain command progress and failures without debug logging\n\nmonochange now uses one progress reporter for workspace loading, validation, linting, publishing, workflow steps, and subprocess output. Interactive terminals can animate, while CI and captured output receive the same events as complete lines without terminal control sequences.\n\nFailures now include a stable diagnostic code, relevant command or file context, and a suggested next action when one is available. Failed checks keep their full result on stdout, so automation can inspect it without searching through stderr.\n\nUse `--progress-format json` for a newline-delimited machine event stream. `--log-level debug` remains available for maintainer tracing and now disables animation so trace records stay readable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #667](https://github.com/monochange/monochange/pull/667)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 💥 Breaking Change\n\n#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)\n\n#### publish readiness verifies trusted publishing and publish order\n\n`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #666](https://github.com/monochange/monochange/pull/666)\n\n#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### Keep check failures and progress output safe for automation\n\n`monochange check` now returns the same non-zero exit status for text, Markdown, JSON, and compact JSON when lint errors exist. JSON callers still receive the complete lint report on stdout.\n\n```bash\nmonochange check --format json\necho $?\n```\n\nBefore this change, a lint failure returned status `0` in JSON modes. It now returns status `1`, which lets CI stop on the failure without parsing `error_count` first.\n\nCaptured workflow progress no longer contains cursor-clearing escape sequences. `MONOCHANGE_NO_PROGRESS=1` also suppresses lint progress as documented, including explicit progress formats.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)\n\n- **Lock every lint rule's autofix behavior behind integration tests.** Every lint rule now has integration coverage that runs `monochange check --fix` against file fixtures and snapshots the fixed manifests, so a rule that deletes or corrupts manifest content can no longer land unnoticed. The fixtures cover all cargo, npm, dart, and changeset rules, including the rules without autofixes whose diagnostics must persist.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #659](https://github.com/monochange/monochange/pull/659) · _Related issues:_ [#658](https://github.com/monochange/monochange/issues/658)\n#### Make human-readable text the default CLI result\n\nmonochange commands now print concise text unless you explicitly request Markdown or JSON. `monochange step config` prints a short workspace summary instead of the complete resolved configuration.\n\n```bash\n# human summary\nmonochange step config\n\n# complete structured configuration\nmonochange step config --format json\n```\n\nExplicit `--format markdown` output stays as raw Markdown even when stdout is a terminal. `--jq` now requires `--format json` or `--format json-min`, so it cannot run a mutating command and only then discover that the result was not JSON.\n\n`--quiet` now controls output only. It no longer silently changes a real operation into a dry run. Add `--dry-run` explicitly when you need both behaviors:\n\n```bash\nmonochange run release --dry-run --quiet\n```\n\nConfigured workflows that intentionally set their own `format` default keep that explicit choice.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)\n\n#### Make package publishing outcomes obvious\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)\n\n#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)\n\n#### Explain command progress and failures without debug logging\n\nmonochange now uses one progress reporter for workspace loading, validation, linting, publishing, workflow steps, and subprocess output. Interactive terminals can animate, while CI and captured output receive the same events as complete lines without terminal control sequences.\n\nFailures now include a stable diagnostic code, relevant command or file context, and a suggested next action when one is available. Failed checks keep their full result on stdout, so automation can inspect it without searching through stderr.\n\nUse `--progress-format json` for a newline-delimited machine event stream. `--log-level debug` remains available for maintainer tracing and now disables animation so trace records stay readable.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #667](https://github.com/monochange/monochange/pull/667)"
		},
		{
			"owner_id": "monochange_analysis",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_analysis/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)"
						]
					},
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)",
							"#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"- **preserve git failure diagnostics during batched revision reads.** `monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #665](https://github.com/monochange/monochange/pull/665) · _Related issues:_ [#664](https://github.com/monochange/monochange/issues/664)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 💥 Breaking Change\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)\n\n#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n- **preserve git failure diagnostics during batched revision reads.** `monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #665](https://github.com/monochange/monochange/pull/665) · _Related issues:_ [#664](https://github.com/monochange/monochange/issues/664)"
		},
		{
			"owner_id": "monochange_cargo",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_cargo/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 💥 Breaking Change\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n### 🐛 Fixed\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
		},
		{
			"owner_id": "monochange_changelog",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_changelog/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🚀 Feature\n\n#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)"
		},
		{
			"owner_id": "monochange_config",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_config/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
		},
		{
			"owner_id": "monochange_core",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_core/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)"
						]
					},
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)",
							"#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)",
							"#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### Preserve structured command output when an operation fails\n\n`MonochangeError::Reported` lets a command return a machine-readable result on stdout and a separate failure diagnostic. Use `reported_output()` at the process boundary to write the result before returning a non-zero exit status.\n\n```rust\nlet error = MonochangeError::Reported {\n\toutput: json_report,\n\tdiagnostic: \"check failed: 1 error\".to_string(),\n};\n\nassert_eq!(error.reported_output(), Some(expected_json));\n```\n\nCallers that only use `Display` or `render()` continue to receive the diagnostic without the attached output.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)",
							"#### Describe every built-in workflow step input\n\n`CliStepDefinition::step_inputs_schema()` now provides non-empty `help_text` for every built-in input. CLIs and agent snapshots built from the schema can explain flags such as `--package`, `--show-all`, `--otp`, `--resume`, and `--sync-provider` without maintaining a second help-text table.\n\n```rust\nlet placeholder = monochange_core::all_step_variants()\n\t.into_iter()\n\t.find(|step| step.kind_name() == \"PlaceholderPublish\")\n\t.expect(\"placeholder step\");\n\nassert!(placeholder\n\t.step_inputs_schema()\n\t.iter()\n\t.all(|input| input.help_text.is_some()));\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)",
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)",
							"#### Make package publishing outcomes obvious\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)",
							"#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 💥 Breaking Change\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n### 🚀 Feature\n\n#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)\n\n#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n#### Keep release notes structured until their output format is known\n\nJSON release-note artifacts now expose summaries, Markdown details, package labels, change types, bump severity, streams, layout, and provenance as separate fields. Text artifacts render those fields directly without Markdown emphasis, while Markdown uses compact list entries for routine changes and expanded sections for breaking changes, migrations, code blocks, and multi-paragraph explanations.\n\nPackage changelogs omit their own package label. Group and workspace changelogs retain package labels so readers can see where each change applies. The built-in section names no longer include emoji; repositories can still opt in by putting emoji in configured section headings.\n\nThe public document types remain source-compatible through a default generic entry type. Callers can opt into structured entries and convert them for an adapter that still expects Markdown strings:\n\n```rust\n// Before: entries were rendered before choosing JSON or text.\nlet notes: ReleaseNotesDocument = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![\"- Keep JSON failures non-zero\".into()],\n    }],\n};\n\n// After: the existing form still works, while typed entries are available.\nlet notes: ReleaseNotesDocument<ReleaseNotesEntry> = ReleaseNotesDocument {\n    title: \"1.2.3\".into(),\n    summary: vec![],\n    sections: vec![ReleaseNotesSection {\n        title: \"Fixes\".into(),\n        collapsed: false,\n        entries: vec![ReleaseNotesEntry {\n            summary: \"Keep JSON failures non-zero\".into(),\n            details_markdown: Some(\"CI can trust the exit status.\".into()),\n            packages: vec![],\n            change_type: Some(\"fix\".into()),\n            bump: BumpSeverity::Patch,\n            stream: \"default\".into(),\n            style: ReleaseNoteEntryStyle::Compact,\n            provenance: ReleaseNoteProvenance::default(),\n        }],\n    }],\n};\nlet legacy = notes.to_legacy_markdown(&ChangelogStyle::default());\n```\n\nThe `changesets/recommended` preset now requires an H1 source summary and rejects a first description sentence that merely repeats it. Enable the standalone check with `\"changesets/summary-description\" = \"error\"` when not using the preset.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #668](https://github.com/monochange/monochange/pull/668)\n\n### 🐛 Fixed\n\n#### Preserve structured command output when an operation fails\n\n`MonochangeError::Reported` lets a command return a machine-readable result on stdout and a separate failure diagnostic. Use `reported_output()` at the process boundary to write the result before returning a non-zero exit status.\n\n```rust\nlet error = MonochangeError::Reported {\n\toutput: json_report,\n\tdiagnostic: \"check failed: 1 error\".to_string(),\n};\n\nassert_eq!(error.reported_output(), Some(expected_json));\n```\n\nCallers that only use `Display` or `render()` continue to receive the diagnostic without the attached output.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #661](https://github.com/monochange/monochange/pull/661)\n\n#### Describe every built-in workflow step input\n\n`CliStepDefinition::step_inputs_schema()` now provides non-empty `help_text` for every built-in input. CLIs and agent snapshots built from the schema can explain flags such as `--package`, `--show-all`, `--otp`, `--resume`, and `--sync-provider` without maintaining a second help-text table.\n\n```rust\nlet placeholder = monochange_core::all_step_variants()\n\t.into_iter()\n\t.find(|step| step.kind_name() == \"PlaceholderPublish\")\n\t.expect(\"placeholder step\");\n\nassert!(placeholder\n\t.step_inputs_schema()\n\t.iter()\n\t.all(|input| input.help_text.is_some()));\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)\n\n#### Make package publishing outcomes obvious\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)\n\n#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
		},
		{
			"owner_id": "monochange_dart",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_dart/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
		},
		{
			"owner_id": "monochange_deno",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_deno/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_ecmascript",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_ecmascript/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
		},
		{
			"owner_id": "monochange_forgejo",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_forgejo/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_gitea",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_gitea/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_github",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_github/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_gitlab",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_gitlab/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_go",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_go/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_graph",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_graph/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_hosting",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_hosting/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_lint",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_lint/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
		},
		{
			"owner_id": "monochange_linting",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_linting/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_npm",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_npm/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🚀 Feature\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)"
		},
		{
			"owner_id": "monochange_publish",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_publish/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### Make package publishing outcomes obvious\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)"
						]
					},
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### publish readiness verifies trusted publishing and publish order\n\n`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #666](https://github.com/monochange/monochange/pull/666)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 💥 Breaking Change\n\n#### Make package publishing outcomes obvious\n\nPackage publishing now starts human-readable output with a distinct outcome such as `Published 3 packages`, `Would publish 3 packages`, `No packages need publishing`, or `Publishing failed for 1 package`. Published or planned package versions appear immediately below the headline, with explicit counts for versions that already exist, are blocked, failed, or were not attempted.\n\nPass `--show-all` to `PublishPackages` or `PlaceholderPublish` when you need every package's status, trusted-publishing metadata, command, stdout, and stderr. JSON and template output always retain all package rows.\n\n`PackagePublishSummary` now reports each domain status directly. Replace `expected`, `succeeded`, and `skipped` with `total()`, `published`, `already_exists`, `blocked`, and `not_attempted` as appropriate:\n\n```rust\nlet summary = report.summary();\nassert_eq!(summary.total(), report.packages.len());\nassert_eq!(summary.published, 3);\nassert_eq!(summary.already_exists, 2);\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #663](https://github.com/monochange/monochange/pull/663)\n\n### 🚀 Feature\n\n#### publish readiness verifies trusted publishing and publish order\n\n`monochange step publish-readiness` now verifies trusted publishing for every selected package before any registry state is mutated, and validates the planned publication order against the workspace dependency graph.\n\n##### Trusted publishing checks\n\nFor each package with `publish.trusted_publishing = true`, readiness now checks:\n\n- the GitHub trust context (`publish.trusted_publishing.repository`, `workflow`, and the optional `environment`) resolves from configuration, source settings, or the CI environment\n- the referenced workflow file exists under `.github/workflows/`\n- the current environment can verify the CI/OIDC identity when a supported CI provider is detected; failures inside a supported provider block the package because the publish would fail outright\n- the package exists on npm, crates.io, and pub.dev. These registries only accept trusted publishing for packages that already exist — a never-published package is blocked with guidance to bootstrap via `monochange step placeholder-publish`. Existing packages are reported as `manual_verification_required` because trusted publisher entries cannot be read back without registry credentials; the report includes the registry setup URL. Registry lookups are skipped (non-blocking) for registries without a public probe and when the network is unavailable.\n\nEach package row in the readiness artifact gains a `trusted_publishing` finding with status `disabled`, `verified`, `manual_verification_required`, or `blocked`; blocked findings block the package and the overall report. Text and Markdown reports render the new column, and local (non-CI) runs degrade identity checks to `manual_verification_required` so the report stays useful during development.\n\nThe artifact schema advances from version 2 to 3; regenerate readiness artifacts after upgrading.\n\n##### Publication order checks\n\nThe artifact now records the dependency-corrected `publish_order` and `order_findings`. The readiness step independently validates the planned order against the workspace dependency graph (including dev-dependencies): a package scheduled before one of its workspace dependencies is a blocking order finding that marks the package as blocked. A release record whose recorded publication order differs from the corrected plan is a non-blocking note, because `publish-packages` follows the dependency-corrected order.\n\n##### Publish-run preflight\n\nReal `monochange step publish-packages` runs now run every readiness checker for all packages before the first publish command executes. A package that cannot publish aborts the run before any registry mutation, instead of failing midway after other packages have already been published. Trusted-publishing project-side checks are registered for every built-in registry, so a package whose trust configuration cannot support the publish is caught in preflight too.\n\n`monochange_publish::registry_client` now bounds every registry request with a 30-second connect timeout and a 60-second total timeout, so an unresponsive registry surfaces as a handled error instead of stalling a publish run or readiness check indefinitely.\n\nNew `monochange_publish` APIs: `registry_package_exists_with_transport` probes package existence on npm, crates.io, and pub.dev; `publish_order_dependency_edges` and `publish_order_dependency_fields` are now public for order validation.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #666](https://github.com/monochange/monochange/pull/666)"
		},
		{
			"owner_id": "monochange_python",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_python/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_schema",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_schema/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "monochange_schema [0.6.0](https://github.com/monochange/monochange/releases/tag/monochange_schema/v0.6.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "💥 Breaking Change",
						"collapsed": false,
						"entries": [
							"#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
						]
					},
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)"
						]
					}
				]
			},
			"rendered": "## monochange_schema [0.6.0](https://github.com/monochange/monochange/releases/tag/monochange_schema/v0.6.0) (2026-09-09)\n\n### 💥 Breaking Change\n\n#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)"
		},
		{
			"owner_id": "monochange_semver",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_semver/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🚀 Feature\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_telemetry",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_telemetry/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "monochange_test_helpers",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "crates/monochange_test_helpers/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)"
		},
		{
			"owner_id": "@monochange/cli",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)",
							"#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n#### `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` no longer replaces whole manifests with a single line\n\nRunning `monochange check --fix` with manifest lint rules enabled could replace an entire `Cargo.toml` with a one-line fragment such as `repository = \"...\"`, deleting every other field, table, and comment in the file. The `cargo/manifest-repository` rule (with or without `allow_workspace_inheritance`) triggered this whenever it rewrote a repository value, and the `cargo/dependency-field-order`, `cargo/internal-dependency-workspace`, and `cargo/sorted-dependencies` fixes carried the same hazard.\n\nThe cargo lint fixes now rewrite the whole manifest from a mutated copy of the parsed document, so unrelated content always survives and toml_edit keeps the surrounding formatting. Whole-file rewrites are additionally validated against the target ecosystem's own manifest parser (`LintSuite::validate_contents`) before anything is written; a rewrite that would produce an unparseable manifest is skipped and the original file is kept. Manifest-level fixes across the npm and dart suites now use the same explicit `LintFix::document` constructor, and new regression tests cover every cargo lint rule's fix output plus an end-to-end `check --fix` convergence run.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #658](https://github.com/monochange/monochange/pull/658)\n\n#### Raise the default publish timeout from 60 to 300 seconds\n\n`cargo publish` waits for crates.io's server-side verification to complete, and that can take minutes when the registry queue is backed up. During the monosecret v0.3.3 release, `cargo publish --locked -p monosecret` needed ~4 minutes and exhausted all three 60-second attempts, failing the release's publish step (and skipping `monosecret_derive`, which depends on the crate being present first).\n\nThe default `publish.timeout.timeout_seconds` is now `300` seconds. The timeout is a ceiling, not a delay: publishes that finish quickly are unaffected, while slow registries no longer report spurious failures (or leave a package unpublished after a run that actually succeeded server-side).\n\nThe schema default is part of the published contract, so the release-record and configuration schemas advance to `v0.6`; the `0.5` → `0.6` migration edge accepts existing records unchanged. Override the default per ecosystem or per package, and set `timeout_seconds = 0` to disable the timeout entirely:\n\n```toml\n[ecosystems.cargo.publish.timeout]\ntimeout_seconds = 300\nretries = 2\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #671](https://github.com/monochange/monochange/pull/671) · _Related issues:_ [#630](https://github.com/monochange/monochange/issues/630)"
		},
		{
			"owner_id": "@monochange/cli-darwin-arm64",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-darwin-arm64/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-darwin-arm64` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-darwin-arm64` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-darwin-x64",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-darwin-x64/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-darwin-x64` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-darwin-x64` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-arm64-gnu",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-linux-arm64-gnu/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-linux-arm64-gnu` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-linux-arm64-gnu` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-arm64-musl",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-linux-arm64-musl/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-linux-arm64-musl` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-linux-arm64-musl` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-x64-gnu",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-linux-x64-gnu/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-linux-x64-gnu` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-linux-x64-gnu` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-linux-x64-musl",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-linux-x64-musl/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-linux-x64-musl` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-linux-x64-musl` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-win32-arm64-msvc",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-win32-arm64-msvc/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-win32-arm64-msvc` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-win32-arm64-msvc` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/cli-win32-x64-msvc",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__cli-win32-x64-msvc/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "Changed",
						"collapsed": false,
						"entries": [
							"#### No package-specific changes were recorded; `@monochange/cli-win32-x64-msvc` was updated to 0.11.0 as part of group `main`."
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### Changed\n\n#### No package-specific changes were recorded; `@monochange/cli-win32-x64-msvc` was updated to 0.11.0 as part of group `main`."
		},
		{
			"owner_id": "@monochange/skill",
			"owner_kind": "package",
			"output": "default",
			"stream": "default",
			"path": "packages/monochange__skill/changelog.md",
			"format": "keep_a_changelog",
			"notes": {
				"title": "[0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)",
				"summary": [],
				"sections": [
					{
						"title": "🚀 Feature",
						"collapsed": false,
						"entries": [
							"#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)",
							"#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)",
							"#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)",
							"#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)"
						]
					},
					{
						"title": "🐛 Fixed",
						"collapsed": false,
						"entries": [
							"#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)",
							"- **preserve git failure diagnostics during batched revision reads.** `monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #665](https://github.com/monochange/monochange/pull/665) · _Related issues:_ [#664](https://github.com/monochange/monochange/issues/664)",
							"- **Teach agents to choose structured output explicitly.** The monochange skill examples now use text for human-facing command defaults and request JSON or Markdown only when the workflow needs that artifact format. The guidance also treats `--quiet` and `--dry-run` as independent choices, matching the CLI contract.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)"
						]
					}
				]
			},
			"rendered": "## [0.11.0](https://github.com/monochange/monochange/releases/tag/v0.11.0) (2026-09-09)\n\n### 🚀 Feature\n\n#### classify Rust compatibility across configured feature and target matrices\n\nCargo packages can opt into cargo-semver-checks during semantic change classification:\n\n```toml\n[ecosystems.cargo.semver_checks]\nenabled = true\ntimeout_seconds = 300\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"default\"\nfeature_mode = \"default\"\n\n[[ecosystems.cargo.semver_checks.matrix]]\nname = \"all-features\"\nfeature_mode = \"all\"\n```\n\nmonochange materializes complete before and after repository trees, runs every configured feature/target cell with an isolated Cargo target directory, and reports the cargo-semver-checks version, exact cell inputs, status, outcome, minimum bump, lint ids, lint titles, and authoritative references. Any proven break proposes `major`, while failed or skipped cells preserve the conservative syntax fallback and require review. A complete non-breaking matrix can replace syntax-derived modifications and removals; added public syntax remains `minor` evidence because cargo-semver-checks does not enable every additive lint by default.\n\nThe change-classification JSON schema is now version `3`. Each finding's `coverage` may include a `checks` array with machine-readable analyzer sub-checks. Pull request comments and text reports render the same cell outcomes and diagnostic summaries.\n\n`monochange_core::EcosystemSettings` now includes `semver_checks`, and `monochange_cargo::CargoSemanticAnalyzer` is no longer a unit struct. Construct it with `monochange_cargo::semantic_analyzer()` for the disabled default or `monochange_cargo::semantic_analyzer_with_settings(settings)` for an explicit matrix.\n\ncargo-semver-checks executes Cargo builds at both endpoints, including build scripts and procedural macros. Run semantic classification only for code you are prepared to execute and without elevated CI or publishing credentials.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #673](https://github.com/monochange/monochange/pull/673)\n\n#### classify TypeScript declaration compatibility with the project compiler\n\n`monochange change classify --detection-level semantic` now resolves each npm package's typed entrypoints, emits isolated before and after declaration surfaces, and asks the workspace's TypeScript compiler whether the consumer contract is breaking, additive, compatible, or inconclusive. Complete breaking evidence proposes `major`, additive evidence proposes `minor`, and compatible implementation-only changes can propose `none`.\n\nInstall `node`, TypeScript, and the package dependencies before classification. Missing tools, invalid configuration, unresolved dependencies, wildcard exports, and identity-sensitive generic or nominal types remain visible as partial evidence with a conservative `patch` proposal and `reviewRequired: true`.\n\nFindings now include the semantic engine, exact engine version, coverage completeness, coverage note, and fallback reason in JSON, text, Markdown, MCP output, job summaries, and pull request comments. The change-classification JSON schema version is now `2`.\n\nThe `monochange_core::SemanticChange` struct is now non-exhaustive and has an optional `assessment`. Custom analyzers should construct findings with `SemanticChange::new` and attach compiler evidence with `with_assessment` instead of using a struct literal:\n\n```rust\nlet change = SemanticChange::new(\n\tSemanticChangeCategory::PublicApi,\n\tSemanticChangeKind::Modified,\n\t\"function\",\n\t\"parse\",\n\t\"function `parse` changed\",\n\t\"src/lib.rs\",\n)\n.with_assessment(SemanticChangeAssessment::new(\n\tSemanticAnalysisOutcome::Breaking,\n\tBumpSeverity::Major,\n\tApiConfidence::High,\n\tSemanticAnalyzerEvidence::new(\n\t\t\"example/analyzer\",\n\t\t\"example-engine\",\n\t\tSemanticAnalysisCompleteness::Complete,\n\t\t\"all public entrypoints checked\",\n\t),\n));\n```\n\nThe semver layer clamps analyzer recommendations to the minimum severity implied by their outcome, so malformed or third-party evidence cannot understate a proven breaking or additive change.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #672](https://github.com/monochange/monochange/pull/672)\n\n#### classify fully deleted packages as breaking changes\n\n`monochange change classify` now discovers packages at both comparison endpoints. A pull request that deletes a package manifest reports the baseline package id, release owner, release tag, and removed API surface even when the candidate also removes its `monochange.toml` entry.\n\nThe report adds a high-confidence `monochange/package-lifecycle/package/removed/package` finding and proposes a major changeset. Package additions produce the corresponding minor finding. `monochange_core::SemanticChangeCategory` now includes `Package` for this lifecycle evidence.\n\nAgents can use the standard command without a manual baseline-discovery fallback:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #670](https://github.com/monochange/monochange/pull/670)\n\n#### classify pull request severity against both the default branch and latest release\n\nAgents and reviewers can now run one command to propose a `major`, `minor`, `patch`, or `none` changeset for every affected package:\n\n```bash\nmonochange change classify --format json --dependency-propagation public\n```\n\nThe versioned JSON report resolves the remote default branch, models the pull request's merge result, finds each release owner's latest reachable tag, and keeps the current pull request recommendation separate from the accumulated release floor. Each package reports compatibility impact, the proposed bump, the enforceable high-confidence minimum, confidence, completeness, pending changesets, the required changeset action, and stable finding ids. Findings include their analyzer, source location, before and after signatures, and the comparisons in which they occur.\n\nMarkdown output contains the same decision evidence for terminal output, job summaries, and pull request comments. The `monochange_classify_changes` MCP tool returns the JSON contract and accepts the same base, head, release, package, and detection controls.\n\nChangeset validation now enforces only high-confidence findings by default:\n\n```bash\nmonochange changeset validate --api --format markdown\n```\n\nUse `--strict` to require pending changesets to satisfy partial or medium-confidence proposals too. A changed package that has no modeled semantic finding now receives a low-confidence patch proposal and a review requirement instead of a false `none` result.\n\n`monochange_analysis::AnalysisSession` is available for tools that compare several git frames. It discovers the package graph once and reuses it:\n\n```rust\nuse monochange_analysis::{AnalysisConfig, AnalysisSession, ChangeFrame};\n\nlet session = AnalysisSession::new(root, AnalysisConfig::default())?;\nlet pull_request = session.analyze(&ChangeFrame::CustomRange {\n\tbase: \"origin/main\".into(),\n\thead: \"HEAD\".into(),\n})?;\n```\n\nExact custom ranges now use `base..head`, while pull request source deltas retain merge-base semantics. Working-directory analysis includes staged, unstaged, deleted, and untracked files. Git revision snapshots use batched object reads, which avoids starting one Git process per file.\n\n`monochange_core::PackagePathMatcher` provides the shared package path policy used by changeset coverage and semantic analysis. Classification honors configured package boundaries, package-level additional and ignored paths, and workspace-level changeset ignores:\n\n```rust\nuse monochange_core::{PackagePathMatch, PackagePathMatcher};\n\nlet matcher = PackagePathMatcher::new(\n\t\"web\",\n\t\"packages/web\".as_ref(),\n\t&[\"shared/schema/**\".into()],\n\t&[\"fixtures/**\".into()],\n);\nassert_eq!(\n\tmatcher.classify(\"shared/schema/api.json\".as_ref()),\n\tPackagePathMatch::Touched,\n);\n```\n\nTypeScript and JavaScript function signatures now preserve object-shaped parameter and return types while excluding implementation bodies, so changes inside those public types are no longer hidden from classification.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #664](https://github.com/monochange/monochange/pull/664)\n\n### 🐛 Fixed\n\n#### refresh docs and validate doc samples in tests\n\nEvery crate's crate-level docs (lib.rs doc comments) now come from the same shared mdt block that feeds its readme, so the two surfaces can no longer drift. Crate docs that had fallen behind the code were rewritten: `monochange_analysis` documents the semantic-analyzer architecture, `monochange_lint` documents the real `Linter`/`lint_workspace` API instead of removed entry points, `monochange_linting` carries the authoring guidance, `monochange_graph` documents the current `build_release_plan` signature with `bump_propagation`, and the `monochange_go`/`monochange_python` intros match the actual adapters.\n\nDocumentation samples that declare a complete `monochange.toml` are now validated in tests against the real configuration loader, which caught and fixed several stale samples: the removed `[release_notes]`/`change_templates`/`extra_changelog_sections` options were replaced with the current `[changelog]` API, knope-migration samples no longer use the unsupported `dependency` versioned-file syntax, and `monochange step placeholder-publish` invocations dropped the removed `--from`/`--output` flags.\n\nCommand references now distinguish `monochange versions sync` from the read-only `versions list` (and mention the deprecation of bare `monochange versions`), the `publish-packages` reference documents `--all`, `--stream-output`, and `--fail-on-duplicate`, the `comment-released-issues` reference documents `--from-ref` and `--auto-close-issues`, the retarget-release reference no longer documents a `format` input the step does not accept, and the knope-migration and `init --provider` claims now match what those commands actually generate. The skill gained the type-scoped `changesets/types/<type>` lint rules from the linting reference.\n\nNew doc-sample validation tests in `monochange_integration_tests` (`docs_code_samples.rs`) parse every fenced `toml` sample in the guide through `load_workspace_configuration`, so documentation samples fail CI when the configuration surface changes.\n\n_Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #656](https://github.com/monochange/monochange/pull/656)\n\n- **preserve git failure diagnostics during batched revision reads.** `monochange_analysis` now waits for `git cat-file` before choosing the error returned by a failed batch read. Callers consistently receive the Git process diagnostic even when the child closes its input pipe before the request writer observes the exit.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #665](https://github.com/monochange/monochange/pull/665) · _Related issues:_ [#664](https://github.com/monochange/monochange/issues/664)\n- **Teach agents to choose structured output explicitly.** The monochange skill examples now use text for human-facing command defaults and request JSON or Markdown only when the workflow needs that artifact format. The guidance also treats `--quiet` and `--dry-run` as independent choices, matching the CLI contract.\n  _Owner:_ [@ifiokjr](https://github.com/ifiokjr) · _Review:_ [PR #662](https://github.com/monochange/monochange/pull/662) · _Related issues:_ [#661](https://github.com/monochange/monochange/issues/661)"
		}
	],
	"provider": {
		"kind": "github",
		"owner": "monochange",
		"repo": "monochange",
		"host": null
	}
}
